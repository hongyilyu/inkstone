# Inkstone — TODO

## Status

**Current phase**: MVP complete
**Last updated**: 2026-05-02 (pi-ai trio bump to 0.72.1)

## Completed

- [x] Bump `@mariozechner/pi-ai`, `@mariozechner/pi-agent-core`, `@mariozechner/pi-coding-agent` from `^0.69.0` to `^0.72.1`. 0.71.0 removed built-in `google-gemini-cli` + `google-antigravity` OAuth providers (zero hits in Inkstone src). 0.72.0 renamed `compat.reasoningEffortMap` → model-level `thinkingLevelMap` and removed the exported `supportsXhigh` capability helper; `availableThinkingLevels` in `src/backend/agent/index.ts` reworked to read `model.thinkingLevelMap?.xhigh !== null` instead (models opt out via explicit null; missing map or any other value means xhigh is offered — matches pi-ai's internal semantics for Opus 4.6/4.7 + GPT-5.2+). pi-kiro@0.1.2 bundles its own `@mariozechner/pi-ai@0.69.0` under nested node_modules; `streamKiro`'s type signature references that bundled pi-ai's `StreamFunction` brand, which differs from Inkstone's hoisted 0.72.1 `StreamFunction` by one union value (`"websocket-cached"` on `SimpleStreamOptions.transport`). Structurally identical at runtime, but the two `AssistantMessageEventStream` identities conflict at the type level. Added a single `streamKiroCompat` cast in `src/backend/providers/kiro.ts:22-40` bridging the two brands — runtime ABI is stable across 0.69→0.72 for the Kiro integration surface (same stream protocol, same event shape). Cast becomes unnecessary once pi-kiro publishes a release that peer-deps pi-ai instead of bundling. pi-ai's Codex default transport flipped from `"sse"` (0.69.0) to `"auto"` (0.72.x) — between this PR and the next (auto-transport opt-in), Codex turns already run on auto (WebSocket first, silent SSE fallback). Test surface unchanged (199 tests green, 3× flake-clean on OAuth-refresh tests). `bun x tsc --noEmit` clean on all files this PR touched; pre-existing errors in `src/backend/persistence/paths.ts`, `test/kiro-refresh.test.ts`, `test/resume-repair.test.ts` unrelated to the bump.

- [x] DialogSelect grouped-category + gutter rendering. Ported the two matching slices from OpenCode's `ui/dialog-select.tsx`: (1) category headers — `DialogSelect` now groups `filtered()` by `category ?? ""` via a `Map`-based `grouped()` memo (insertion-order preserving; callers whose `options` are already contiguous-per-category — e.g. DialogModel's `connectedProviders().flatMap(...)` — see byte-identical visible order), renders a `theme.accent`-bold header box above each non-empty group with `paddingTop={1}` between consecutive groups, and a new `rows()` memo accounts for header + spacer lines so `height()` sizes the scrollbox correctly; `flat()` (navigation index space) stays derived from `grouped().flatMap(([, opts]) => opts)`. (2) `gutter?: JSX.Element` on `DialogSelectOption` — fixed-width slot left of the title, mutually exclusive with the `●` current-indicator (the `current()` Show and the `!current() && option.gutter` Show are sequential and disjoint); row `paddingLeft` widens from `current() ? 1 : 3` to `current() || option.gutter ? 1 : 3` so the gutter occupies the same 2-col lane as `●`. DialogProvider (in `src/tui/components/dialog/provider/index.tsx` after the master-side dialog restructure) now surfaces connection state via `gutter: <text fg={theme.success}>✓</text>` on connected providers (empty gutter on disconnected; sort already puts unconnected rows last, so no description text needed). DialogModel (`src/tui/components/dialog/model.tsx`) drops its `description: provider.displayName` since the provider name now renders once as the group header — with Kiro disconnected, only "Amazon Bedrock" shows today; once a second provider connects they both appear with matching headers. Deliberately deferred from OpenCode's `dialog-select.tsx`: `skipFilter`, `keybind[]`, `selectedForeground`, `getScrollAcceleration`, `disabled`, `footer`/`margin`/`categoryView` slots, `DialogSelectRef`, `onMove`/`onFilter`, `flat` mode toggle, per-option `onSelect` — each waits for a real caller. Dormant caveat documented in the `rows()` comment: if a future caller mixes uncategorized (empty-string bucket at index 0) and categorized (non-empty at index ≥1) options in one DialogSelect, the spacer count inflates by one; neither current caller mixes. `test/preload.ts` now seeds `AWS_PROFILE=default` so `isConnected()` is hermetic across dev/CI regardless of `~/.aws/` presence. Two new tests in `test/tui/dialogs.test.tsx`: (a) Connect dialog renders `✓` and no `"Not configured"`/`"✓ Connected"` text, (b) Select Model header position — `"Amazon Bedrock"` appears above `"Nova"` (substring-index check, not a tautological presence check). `bun run ci` passes; 3× back-to-back runs clean.

- [x] Totals persistence across session restores. `resumeSession` previously zeroed `store.totalTokens` / `store.totalCost`, so reopening a session via Ctrl+N → Enter showed "0 tokens / $0 spent" next to N prior turns even though per-turn `AssistantMessage.usage` has always been persisted inside `agent_messages.data` (JSON blob, one row per raw AgentMessage). Fix folds the usage sum in `loadSession`: `LoadedSession` gains `totals: { tokens: number; cost: number }`, computed over the pre-repair `agentMessagesOut` list so synthesized alternation-repair placeholders (which have no `usage`) can't contribute. The TUI's `resumeSession` in `src/tui/context/agent.tsx` swaps the two literal-zero `setStore` calls for `setStore("totalTokens", loaded.totals.tokens)` / `setStore("totalCost", loaded.totals.cost)`. `clearSession` keeps zeroing (a new in-memory session starts fresh). `lastTurnStartedAt` stays zeroed on resume — it's a current-turn stopwatch, not a rollup. Defensive: `cost += m.usage.cost?.total ?? 0` guards against a provider writing `usage` without a cost breakdown (pi-ai types `cost.total` as non-optional but `+ undefined` would NaN-poison the rollup); the live `message_end` accumulator in `tui/context/agent.tsx` adopts the same `?? 0` guard so the two paths stay in lockstep. Aborted assistant rows carrying partial `usage` contribute their real tokens — those tokens were paid for; this is intentional behavior, not a leak. No schema change, no migration, no new writer. Session-level cost is a pure derivation from `agent_messages.data`, which stays the single source of truth. Test: new `test/resume-totals.test.ts` (5 cases) — multi-turn sum with `toBeCloseTo`, empty session → 0/0, synthesized placeholder contributes 0, assistant missing `usage` contributes 0 without NaN, assistant with `usage` but missing `cost.total` contributes tokens but 0 cost. `bun run ci` passes (179 tests across 21 files, from 174). TUI char-frame integration test deliberately skipped — `resumeSession`'s three-line seed has no branching, and sidebar formatting is already covered by existing non-zero-totals tests.

- [x] Per-message reasoning-effort stamping on `DisplayMessage`. Addresses the gap in the per-model effort feature: the effort was session-scope only, rendered on the prompt statusline but nowhere on completed bubbles, so after a mid-session model switch there was no record of which effort produced which historical reply. `DisplayMessage` gains an optional `thinkingLevel?: ThinkingLevel` field, stamped in `agent_end` on the turn-closing bubble (same site, same shape as `duration`). Renderer in `src/tui/components/message.tsx` suffixes the assistant footer with `· <level>` in `theme.warning` bold when set and non-off — mirrors the prompt statusline badge for a single visual language. **Snapshot-at-turn-start contract**: the reducer captures `store.thinkingLevel` inside `wrappedActions.prompt`'s `onSuccess` block (next to `lastTurnStartedAt`) into a provider-local `turnStartThinkingLevel`, then applies it at `agent_end`. Reading `store.thinkingLevel` at event time would let a mid-stream `setThinkingLevel` / `setModel` relabel the historical bubble — the snapshot insulates the stamp from that drift. `"off"` is deliberately NOT persisted: NULL in the DB and `"off"` in the store render identically (no badge), so conflating them is lossless and keeps the column NULL for the majority of non-reasoning sessions. Interrupted turns (new `interrupted?: boolean` field from the abort/error split) also skip the stamp — same rationale as skipping the duration pip: the reply didn't complete, "what effort produced this turn?" has no meaningful answer. Schema: `messages.thinking_level TEXT` nullable, regenerated `0000_init.sql` + snapshot (pre-1.0 nuke mandate — no migration path, dev DBs deleted manually). Writers (`appendDisplayMessage`, `updateDisplayMessageMeta`) gain one line each; `loadSession` casts the opaque TEXT through `ThinkingLevel | null` on read (same posture as `agentName`/`modelName`, which are also unvalidated TEXT — the Zod enum in `config/schema.ts` gates writes at... (line truncated to 2000 chars)

- [x] Disconnect / re-auth actions in the Connect dialog. Selecting a *connected* Kiro row previously dismissed the dialog as a no-op — a user with wedged creds, a different AWS identity, or simply a desire to sign out had no in-app path. Kiro-only scope: Inkstone owns Kiro creds (`~/.config/inkstone/auth.json`), so "disconnect" actually means something; Bedrock creds live in `~/.aws/` / `AWS_*` env vars, so a Bedrock manage menu would be a lie (kept today's dismiss-on-select). New flow: connected-Kiro-select opens a two-option `DialogSelect` (Reconnect, Disconnect) via `showManageMenu`. Reconnect delegates to the existing `startKiroLogin(...)` — no pre-clear because `loginKiro` doesn't read existing creds and `saveKiroCreds` overwrites atomically on success, so ESC mid-login leaves creds intact (non-surprising). Disconnect routes through `DialogConfirm` (destructive guard) → `clearKiroCreds()` → optional active-provider rehome → toast. Active-provider rehome: when `activeProviderId === "kiro"` AND `DEFAULT_PROVIDER` (Bedrock) `isConnected()`, the dialog calls `onModelSelected(resolveModel(bedrock.id, bedrock.defaultModelId))` so the next prompt doesn't 401 against the just-cleared Kiro creds. Mirrors the boot-time fallback chain in `resolveInitialProviderModel` (`src/backend/agent/index.ts`). When Bedrock is *also* disconnected, the toast downgrades to a warning ("Pick a new model from /models before your next prompt") and the active pick is left alone — the user re-picks manually. `DialogProvider.show` signature grew one optional `activeProviderId?: string` — backwards-compatible for any caller without a session handle (existing tests pass `undefined` implicitly; the rehome branch no-ops). Single caller in `app.tsx:168-175` passes `session.getProviderId()`. `useAgent()` is deliberately NOT called from inside `DialogProvider` because the dialog stack sits above `AgentProvider` in the provider tree (`app.tsx:377-389`) — threading the id through `show` avoids that co... (line truncated to 2000 chars)

- [x] Differentiate aborted turns from hard errors visually. Previously both `stopReason === "error"` and `"aborted"` funneled into the same warning-bordered panel below the assistant body; the pre-existing `[Interrupted by user]` marker below the user bubble partially covered the abort case, but inside the assistant bubble itself the two outcomes were indistinguishable. Fix ports OpenCode's `MessageAbortedError` split trimmed to Inkstone's scope: `DisplayMessage` grows a `interrupted?: boolean` field alongside the existing `error?: string`; the reducer's `message_end` branch in `tui/context/agent.tsx` now routes `stopReason === "error"` to `error` (red panel) and `stopReason === "aborted"` to `interrupted: true` (no panel). `AssistantMessage` in `message.tsx` gates the red-bordered error panel on `msg.error` alone; the footer gains a trailing `<Show when={msg.interrupted}> · interrupted</Show>` span in `theme.textMuted`, tints the `▣` glyph `textMuted` (instead of the agent color) on abort, and the outer `<Show>` widens to `msg.modelName || msg.interrupted` so a very fast abort that never stamps `modelName` still renders a bare `▣ Reader · interrupted` footer. The outer list gate in `conversation.tsx` and `isDanglingUser`'s "real assistant" predicate in `message.tsx` both widen from `parts.length > 0 || error` to `parts.length > 0 || error || interrupted` — required so an empty-parts aborted bubble is still visible and doesn't mask the dangling-user marker with an invalid "real reply follows" signal. The `agent_end` duration-stamp branch gates on `!last.interrupted` so aborted turns skip the duration pip — a wall-clock-until-abort value would misread as a completed-turn duration next to the ` · interrupted` suffix (mirrors OpenCode's behavior). Persisted through the `messages` table via a new `interrupted INTEGER` column (Drizzle `integer({ mode: "boolean" })`), round-tripped in `appendDisplayMessage` / `updateDisplayMessageMeta` / `loadSession`'s `displayMessages`... (line truncated to 2000 chars)

- [x] Slash-command parser hardening — strict start-of-buffer gate. `prompt.tsx:handleSubmit`'s slash branch previously tested `value.startsWith("/")` against `value = text().trim()`, so a buffer with any leading whitespace (`  /clear`, `\t/clear`, `\n/clear`) dispatched the slash — a pasted message that happened to contain `/clear` after whitespace could wipe a session. The autocomplete dropdown's open rule (`t.startsWith("/")` in `prompt-autocomplete.tsx:261`) and the coaching-hint memo (`prompt.tsx:326`) already read the untrimmed buffer; the submit path was the outlier. Fix swaps the guard to `rawText.startsWith("/")` so open / dispatch / hint all agree on strict-SOF. The `rawText.indexOf("/")` anchor + `slashStart` local become a dead constant and are removed; `indexOf(" ", slashStart)` collapses to `indexOf(" ")`, `slice(slashStart + 1, …)` to `slice(1, …)`. The load-bearing comment block explaining why the anchor existed (carried over from the earlier "compose `/article` with @-mentions" fix) is rewritten to name the new strict-SOF contract and the open / dispatch / hint parity it restores. Multi-line slash buffers (`/article\n<body>`) keep today's fallthrough-as-plain-prompt behavior — `indexOf(" ")` on a newline-only-whitespace buffer returns -1 and the bare-name branch sees `article\n<body>` as the name, which `canRunSlash` rejects. Not a designed contract, so no test pins it. One new case in `test/tui/autocomplete.test.tsx`: `  /clear` submitted, `fake.calls.clearSession === 0`, `fake.calls.prompt[0] === "  /clear"` (leading whitespace preserved in the plain-prompt fallthrough). `bun run ci` passes (175 tests across 20 files); 3× back-to-back runs on the new test clean.


## Completed

- [x] DialogSelect grouped-category + gutter rendering. Ported the two matching slices from OpenCode's `ui/dialog-select.tsx`: (1) category headers — `DialogSelect` now groups `filtered()` by `category ?? ""` via a `Map`-based `grouped()` memo (insertion-order preserving; callers whose `options` are already contiguous-per-category — e.g. DialogModel's `connectedProviders().flatMap(...)` — see byte-identical visible order), renders a `theme.accent`-bold header box above each non-empty group with `paddingTop={1}` between consecutive groups, and a new `rows()` memo accounts for header + spacer lines so `height()` sizes the scrollbox correctly; `flat()` (navigation index space) stays derived from `grouped().flatMap(([, opts]) => opts)`. (2) `gutter?: JSX.Element` on `DialogSelectOption` — fixed-width slot left of the title, mutually exclusive with the `●` current-indicator (the `current()` Show and the `!current() && option.gutter` Show are sequential and disjoint); row `paddingLeft` widens from `current() ? 1 : 3` to `current() || option.gutter ? 1 : 3` so the gutter occupies the same 2-col lane as `●`. DialogProvider now surfaces connection state via `gutter: <text fg={theme.success}>✓</text>` on connected providers (empty gutter on disconnected; sort already puts unconnected rows last, so no description text needed). DialogModel drops its `description: provider.displayName` since the provider name now renders once as the group header — with Kiro disconnected, only "Amazon Bedrock" shows today; once a second provider connects they both appear with matching headers. Deliberately deferred from OpenCode's `dialog-select.tsx`: `skipFilter`, `keybind[]`, `selectedForeground`, `getScrollAcceleration`, `disabled`, `footer`/`margin`/`categoryView` slots, `DialogSelectRef`, `onMove`/`onFilter`, `flat` mode toggle, per-option `onSelect` — each waits for a real caller. Dormant caveat documented in the `rows()` comment: if a future caller mixes uncategorized (empty-string bucket at index 0) and categorized (non-empty at index ≥1) options in one DialogSelect, the spacer count inflates by one; neither current caller mixes. `test/preload.ts` now seeds `AWS_PROFILE=default` so `isConnected()` is hermetic across dev/CI regardless of `~/.aws/` presence. Two new tests in `test/tui/dialogs.test.tsx`: (a) Connect dialog renders `✓` and no `"Not configured"`/`"✓ Connected"` text, (b) Select Model header position — `"Amazon Bedrock"` appears above `"Nova"` (substring-index check, not a tautological presence check). `bun run ci` passes (176 tests); 3× back-to-back runs clean.

- [x] Filter Bedrock Anthropic IDs that can't be invoked on-demand. Raw `anthropic.claude-*` IDs on Bedrock require an inference profile (e.g. `aws bedrock create-inference-profile` or the Bedrock console) — selecting one in `DialogModel` failed at first stream with `ValidationException: Invocation of model ID ... with on-demand throughput isn't supported`. Added an `isOnDemandBedrockModel` filter in `src/backend/providers/amazon-bedrock.ts` that drops any model whose id matches `/^anthropic\./`. Regional-prefix variants (`us.`, `eu.`, `apac.`, `global.`) stay listed; other vendors (`amazon.nova-*`, `meta.llama*`, `deepseek.*`, etc.) pass through. Before the filter pi-ai returned 91 Bedrock models, 14 of which were on-demand-incapable raw Anthropic IDs; after, 77 models survive. The curated `defaultModelId` (`us.anthropic.claude-opus-4-7`) survives the filter — a regression test in `test/bedrock-filter.test.ts` pins this against the live pi-ai registry so a future pi-ai rename surfaces there before the agent module's own boot throw. Test: 5 cases covering raw-anthropic rejection, regional-prefix acceptance, non-Anthropic pass-through, `listModels()` post-filter invariants, and defaultModelId survival. `bun run ci` passes (144 tests across 17 files); `bun x tsc --noEmit` clean. Documented in `Future Work` that when pi-ai adds a per-model `onDemand: boolean` flag, the filter can be replaced with `m.onDemand !== false`.

- [x] Move tool-arg rendering contract into `src/bridge/`. `src/tui/util/tool-summary.ts` hard-coded a `switch(name)` over the four shipped tools (read/write/edit/update_sidebar) plus a truncated-JSON fallback. Per-tool display knowledge lived inside the UI layer, so a third-party agent couldn't add a tool without editing `tui/` code and `ToolPart` was coupled to the tool vocabulary. Moved the renderers into `src/bridge/tool-renderers.ts` as `TOOL_ARG_RENDERERS: Record<string, (args: unknown) => string>` + a thin `renderToolArgs(name, args)` lookup. `bridge/` is the shared type contract between backend and frontend and has zero runtime deps — it's exactly where a display contract belongs. The pure helpers (`trunc`, `str`, `arr`, `asObj`) moved alongside; four entries (read/write/edit/update_sidebar) match today's behavior byte-for-byte. Unknown tool → empty string (no args row, just the tool name) — drops the old JSON-truncation fallback per plan; the only way to hit the empty path is to add a tool without adding a renderer, and an empty string nudges the contributor to supply one. `src/tui/components/message.tsx`'s `ToolPart` swaps the import to `@bridge/tool-renderers` and calls `renderToolArgs`. `src/tui/util/tool-summary.ts` deleted. Test: new `test/tool-renderers.test.ts` pins each renderer (12 cases) — path / filePath fallback, edit-count singular/plural, truncation at 80 chars, unknown-tool empty string, null/non-object args don't throw, write stays distinct from edit. `bun run ci` passes (139 tests across 16 files); `bun x tsc --noEmit` clean.

- [x] Secondary page content format field. `SecondaryPageState` gains an optional `format?: "markdown" | "text"` discriminator; default stays `"markdown"` so existing callers (reader's `/article`, `@`-mention previews via `file-part-handler.ts`) keep today's rendering. The new `"text"` branch in `components/secondary-page.tsx` uses a `<Show>` guard to route plain-text content through a raw `<text>` node with `wrapMode="word"` instead of the markdown parser — unblocks future subagent work output, logs, or any non-markdown content that the markdown renderer would mangle (heading markers, literal bullets, etc.). No existing call-site changes needed; the field is optional with a documented default. Test: new case in `test/tui/secondary-page.test.tsx` opens a page with `format: "text"` and `content: "# not a heading\n* not a bullet"`, asserts the literal `#` and `*` glyphs survive in the char frame (the markdown renderer would have consumed them). `bun run ci` passes (127 tests); 3× back-to-back runs on the new test clean.

- [x] Slash extra-args guard. `/clear my cache` previously fired `/clear` and silently dropped " my cache" — the dispatcher matched on `name` alone and treated trailing text as harmless. Fix extends the existing `canRunSlash`/`triggerSlash` guards in `src/tui/components/dialog-command.tsx` with a second rule: reject when a command sets neither `takesArgs` nor `argHint` but args is non-empty. Reuses the existing `argHint`-as-accepts-optional-arg signal (see SlashSpec JSDoc), so `/article` continues to accept trailing text while `/clear` rejects it. Both guard checks extracted into a private `canRunSlashEntry(entry, args)` helper so `canRunSlash` and `triggerSlash` stay in sync. No caller changes — `prompt.tsx` already falls through to the plain-prompt path when `triggerSlash` returns false. One new test case in `test/tui/autocomplete.test.tsx` pins the behavior: `/clear my cache` submits as a literal prompt, `fake.calls.clearSession === 0`. `bun run ci` passes (126 tests); 3× back-to-back runs on the new test clean.

- [x] UI security hardening — symlink reject on file-chip click + ANSI sanitization in tool-arg rendering (M3 + M4 from May 2026 audit). Two defense-in-depth gaps in the TUI path: (M3) `openVaultFilePart` in `src/tui/util/file-part-handler.ts` ran `isInsideDir(abs, VAULT_DIR)` string-level sandbox but did NOT `lstatSync` the target, so a symlink *inside* the vault pointing *outside* (e.g. `Articles/sneak.md → /etc/hosts`) got followed by `readFileSync` and its content rendered in the secondary page. Both current producers (`/article`, `@`-mentions) reject symlinks write-side, but `file` parts round-trip through SQLite, so a persisted chip could outlive the write-side guard if a symlink was planted in the vault later. (M4) `summarizeToolArgs` in `src/tui/util/tool-summary.ts` renders LLM-emitted args via `<text>` (not `<markdown>`), so an LLM that emits `args.path = "\x1b[2Jhi"` could clear the screen on a tool-part render. Fix M3: `lstatSync(abs)` + reject on `isSymbolicLink() || !isFile()` with the existing error-page fallback — mirrors `readFileSafe`'s write-side guard in `mentions.ts`. Fix M4: new `src/tui/util/ansi.ts` exposes `stripAnsi(s)` (CSI + C0 control-byte scrub, preserves TAB/LF/CR); `tool-summary.ts`'s `trunc()` applies it to every case (`read`/`write`/`edit`/`update_sidebar` + generic JSON fallback). Scope deliberately narrow: OSC/DCS sequences are not scrubbed (rare in LLM output, no observed vector); `DisplayMessage.error` / `DisplayPart.error` paths are not scrubbed (those come through pi-ai's provider-error path, which providers generally sanitize — lower-severity item from the audit, deferred). New `test/ui-hardening.test.ts` (11 cases): CSI/C0/bare-ESC strip, plain-text passthrough, idempotency; sanitization for `read`/`update_sidebar` + JSON fallback; symlink reject for the preload's `sneak.md` case, a scratch-created inside-vault symlink to an outside file, and a positive case that a regular file still opens. `afterEach` closes the secondary page so the module-global signal doesn't leak a stale "Cannot open" frame into other test files' `waitForFrame` assertions. `bun run ci` green (153 tests).

- [x] CI hygiene — test-dir linting + dependency audit hook (H6 / H7 from May 2026 audit). Two gaps in the CI surface: (H6) `bun audit` reports 10 advisories, 5 high — the `solid-js@1.9.9 → seroval<=1.4.0` chain is the biggest cluster (prototype pollution, RCE, 2× DoS, RegExp DoS), but no CI step was surfacing them. (H7) `biome.json` `files.includes` targeted `src/**/*.{ts,tsx}` only — the `test/` tree was invisible to lint, so `AGENTS.md`'s "bun run ci is the quality gate" claim was silently scoped to source only. Tests could introduce boundary violations, `any` drift, or format noise without tripwire. Fix: extend `files.includes` to `["src/**/*.{ts,tsx}", "test/**/*.{ts,tsx}"]` and drop the redundant `./src` argv from `package.json` `format`/`lint`/`check`/`ci` scripts so `biome.json` is the single source of truth. A new `test/**` overrides block disables `noNonNullAssertion` — test code routinely narrows `loadSession(id)!` or `readerAgent.commands!` after a sibling `toBeDefined` assertion, and the Biome `!` warning conflicts with that pattern. Converted one load-bearing case (`readerAgent.commands!.find`) to a proper `if (!articleCommand) throw` guard so the type narrowing is semantic, not a suppression. Removed 9 now-orphaned `// biome-ignore lint/style/noNonNullAssertion: guarded by first test` comments from `test/permissions.test.ts` that had been added in earlier passes. New `audit` script (`bun audit`) documented in `AGENTS.md` as a periodic check — NOT wired into `bun run ci` because the current 10-advisory set is upstream-blocked: `@opentui/solid@0.1.104` pins `solid-js: 1.9.11` as an *exact-version peer dependency*, and solid-js 1.9.11 itself depends on `seroval: ~1.3.0`, so no patched seroval is reachable without forking either package. Wiring audit into ci would fail-shut on every run with no actionable fix. The advisories are now documented in Known Issues below so they're visible but not blocking. Solid-js 1.10 doesn't exist (latest is 1.9.x + 2.0.0-experimental.N); `overrides` on seroval would violate solid-js's `~1.3.0` range check. Test scripts targeting `./src` argv (`format`/`lint`/`check`) would silently re-scope to source-only and regress test-dir coverage, so the argv is deliberately absent — documented in `AGENTS.md` Pre-Commit Protocol. `bun run ci` green (142 tests, biome clean across src + test).

- [x] Atomic writes + centralized dir mode for config + auth (H2/H3/H4 from May 2026 audit). Three related hazards on `~/.config/inkstone/`: (H2) `writeFileSync` is `open(O_TRUNC) + write` — a SIGKILL or disk-full mid-write truncates `config.json` / `auth.json`, next load sees invalid JSON, every preference / refresh token is silently lost on fallback to defaults; (H3) `writeFileSync({ mode: 0o600 })` only honors mode on creation, so overwriting a pre-existing 0644 `auth.json` leaves the refresh-token bytes on disk at 0644 for the duration of the write, then tightens via a follow-up `chmodSync` — a concurrent local reader can race-read during the window; (H4) `config.ts`'s `mkdirSync(CONFIG_DIR, { recursive: true })` uses default umask (0755), `auth.ts`'s uses `0700` — whichever writer creates the dir first wins, leaving `~/.config/inkstone/` world-listable if `saveConfig` runs before `saveKiroCreds`. Fix centralizes two primitives in `paths.ts`: `ensureConfigDir()` (creates `CONFIG_DIR` at 0700, always `chmodSync`s to tighten a pre-existing 0755) and `writeFileAtomic(dest, data, mode)` (writes to `${dest}.tmp` via `O_CREAT | O_EXCL | O_WRONLY` with the exact mode, `fsync`s, closes, `rename`s atomically). `O_EXCL` is deliberate: an orphaned `.tmp` from a prior crash fails loudly on the next save rather than silently clobbering, so the user sees the error in a toast instead of losing the file. `config.ts` `saveConfig` and `auth.ts` `save` both switch to `ensureConfigDir()` + `writeFileAtomic`; `auth.ts` drops the redundant trailing `chmodSync` (the tmp-file mode is now authoritative). `config.json` mode is tightened from 0644 to 0600 to match the "every file in this dir is sensitive" posture — no secrets today, but the dir already lives under 0700 so the tightening costs nothing and defends against future additions. New `test/persistence-atomic.test.ts` (8 cases): atomic-write mode on create, overwrite preserves mode with no 0644 window, orphaned `.tmp` fails loudly (O_EXCL), unbounded content size atomicity, `ensureConfigDir` idempotency, 0755 → 0700 re-tightening, `saveConfig` end-to-end produces 0600 file in 0700 dir. Scratch dir isolated from the preload's `CONFIG_DIR` so the vault pointer in `config.json` survives the test run; the `saveConfig` block re-seeds the preload's config after each test so downstream tests still see the vault. `bun run ci` green (140 tests).

- [x] Narrow path-resolver parity with pi-coding-agent (H5 from May 2026 audit). `dispatchBeforeToolCall`'s inline `resolvePath` now folds `UNICODE_SPACES` (NBSP `\u00A0`, `\u2000`-`\u200A`, narrow no-break `\u202F`, medium math `\u205F`, ideographic `\u3000`) to regular spaces **before** `~` expansion and `resolve(VAULT_DIR, …)`, mirroring the first step pi-coding-agent's tool path performs (`dist/core/tools/path-utils.js:4-8`). Without this, an LLM-emitted `010\u00A0RAW/…` routed through the sandbox against its pre-normalized bytes while the tool saw the post-normalized file — the sandbox decision could diverge from what the filesystem actually opened. Scope was intentionally kept narrow: `path-utils.js` is not in pi-coding-agent's `exports` map (only `.` and `./hooks` are), so deep-import isn't viable; a minimum inline port covers the real drift. The NFD/curly-quote/AM-PM variant hunt from pi-coding-agent's `resolveReadPath` is deliberately **not** mirrored — those exist for agent-authored filenames on macOS, and Inkstone's vault is user-owned, so mirroring them would expand the sandbox surface without closing a real hole. Two new cases in `test/permissions.test.ts`: (1) `010\u00A0RAW/013 Articles/foo.md` routes through the Articles zone confirm rule (proves zone membership survives normalization), (2) `/tmp\u202Ffoo/bar.md` is still blocked by the `insideDirs [VAULT_DIR]` baseline (guards against the normalization accidentally letting an outside-vault path through). `bun run ci` green (125 tests).

- [x] Harden load-time alternation repair against tool-result interior gaps and sequential dangling placeholders (C2 from May 2026 audit). Two bugs on the same repair pass in `loadSession`: (a) the neighbor check was `repaired[length - 1]`, which `toolResult` / `custom` rows between two `user`s would mask — a `[user, toolResult, user]` triple escaped repair and handed Bedrock consecutive user turns on the next prompt; (b) `findLatestAssistant` returned any assistant including synthesized placeholders, so sequential dangling gaps compounded — the second placeholder inherited `model: "placeholder"` from the first rather than bland-default falling back. Fix replaces the tail lookup with `lastAlternationRole(repaired)` which scans backward to the last `user | assistant` role (skipping `toolResult` / `custom`), and renames `findLatestAssistant` → `findLatestRealAssistant` which additionally skips synthesized placeholders (identified by `stopReason === "aborted" && errorMessage === INTERRUPTED_MARKER`). Extracted the `"[Interrupted by user]"` literal to a module-local `INTERRUPTED_MARKER` so `buildAbortedAssistant` and `findLatestRealAssistant` stay in sync. Real user-aborted bubbles match the same skip predicate, but their `api`/`provider`/`model` would propagate correctly anyway (same provider, same model, no functional change), so the widening is harmless — accepted behavior. Two new tests in `test/resume-repair.test.ts` (8 total): (1) `[user, toolResult, user]` triggers repair between the users, (2) three sequential dangling gaps each produce placeholders with bland defaults instead of propagating `"placeholder"` forward. All 8 repair tests pass; `bun run ci` green (67 total).

- [x] Fix Kiro refresh race + pi-agent-core `getApiKey` contract (C1 + H1 from May 2026 audit). Concurrent `getApiKey()` calls now share a single in-flight refresh promise via a module-level `inflight: Promise<KiroCredentials | undefined> | null` memo — two near-simultaneous turns (two streams, two tabs, post-sleep wake) no longer race the same refresh token against AWS SSO-OIDC, which would otherwise rotate the token and make the loser hit `invalid_grant` → `clearKiroCreds` → mid-session forced re-login. The memo clears via `.finally` so a failed refresh doesn't pin the slot. Refactored the refresh body into a private `doRefresh()` so the memo is a pure 5-line wrapper. Also fixed the pi-agent-core contract violation: `refreshIfNeeded`'s catch branch previously threw (surfacing as a generic error bubble instead of the intended Connect-dialog toast); now it calls `reportPersistenceError({ kind: "auth", action: "refresh", ... })` and returns `undefined`, honoring pi-agent-core's documented "must not throw or reject" contract for `getApiKey`. Raw cause is `console.error`-logged for debug but kept out of the user-facing toast (defense against a future pi-kiro upstream change that might embed token bytes in the fetch error). New `test/kiro-refresh.test.ts` covers (a) concurrent-dedup (two `getApiKey()` calls resolve to the same access token with `refreshKiroToken` mocked to delay + count calls), (b) no-throw on refresh failure, (c) `isConnected()` correctly returns false after a clear. Mock installed via `bun:test`'s `mock.module("pi-kiro/core", ...)` before the provider import; `saveKiroCreds`/`clearKiroCreds` used for seed-expired fixtures so the auth-module cache stays in sync with disk.

- [x] TUI test suite — tail-gap sweep. A third cross-reference audit surfaced 7 reducer/routing branches the first two passes missed; all now covered. Added: `streaming.test.tsx` "update_sidebar delete op removes the section" (distinct from the upsert branch tested previously — `sections.filter(s => s.id !== d.id)` in the reducer); `prompt.test.tsx` "/clear wipes dynamic sidebar sections" (ephemeral-state wipe on `wrappedActions.clearSession`); `session-list.test.tsx` "resume wipes dynamic sidebar sections from the previous session" (same wipe in `resumeSession` batch, seeded-raw-agent-message style matches the existing Enter-resumes case for consistency); `conversation.test.tsx` parameterized redacted-thinking via `test.each` covering both `[REDACTED]` (OpenRouter literal) and `Reasoning hidden by provider` (pi-kiro slow-path marker §26a) — two cases from one source; `interrupt.test.tsx` "interrupt arm resets when turn ends before the second ESC" (pins the `createEffect` in prompt.tsx that clears the counter on `isStreaming → false`, documented in the TODO as intentional divergence from OpenCode); `secondary-page.test.tsx` "ESC with an open dialog over a secondary page closes the dialog, not the page" (the `dialog.stack.length === 0` routing guard in app.tsx); `conversation.test.tsx` "sidebar Context block renders token count and cost after a turn with usage" (the visible right-panel usage rendering gated on `hasUsageData` memo). Fake-session enhancement: `assistantMessage()` now deep-merges `usage` and `usage.cost` so tests can pass partial overrides (`{ totalTokens: 1234, cost: { total: 0.05 } }`) without spelling out the full object — other tests are unaffected because partial usage was previously not used. 125 tests across 15 files, `bun run ci` green, `bun x tsc --noEmit` clean. Items deferred from this pass (accepted): theme actually repaints (color-level), persistence-error toast surface (covered at backend layer), DialogConfirm y/n shortcut (seam-widening disproportionate), real `setUser`/redactions flag handlers (orthogonal to reducer), per-message reasoning-effort rendering (deferred TODO).

- [x] TUI test suite — coverage gap sweep. A second cross-reference audit against every TUI-touching TODO entry turned up 6 meaningful regression-risk gaps; all are now covered. Added: `conversation.test.tsx` "user bubble with `@`-mention renders file chip (`[md]` + path)" (covers the user-bubble chip rendering the `/article` compact-bubble feature ships, also covers `@`-mention chip rendering); `autocomplete.test.tsx` "bare `/article` opens a recommendation picker" (exercises `helpers.pickFromList` → DialogSelect "Recommended articles"), "mention whose backing file is gone surfaces `Could not read` toast" (failed-read fallback path), "slash dropdown open suspends Ctrl+N" (already shipped; documented here for completeness); `agent-cycle.test.tsx` "`/article` falls through as plain prompt on a non-reader agent" (cycles to Example, asserts text passes as plain prompt); `dialogs.test.tsx` "Effort palette entry is hidden for non-reasoning models", "Effort entry visible for reasoning model; opens DialogVariant" (reactive-visibility invariant + dialog round-trip; requires `FAKE_MODEL.reasoning = true` in one test), "open palette suspends Ctrl+N" (dialog-driven keybind suspend — the complement of the autocomplete-driven case). 118 tests across 15 files, `bun run ci` green, `bun x tsc --noEmit` clean. Removed the untracked `test/kiro-refresh.test.ts` + unrelated `src/backend/providers/kiro.ts` local edits that were left over from a separate audit and broke against the installed pi-kiro package.


- [x] TUI test suite — coverage expansion pass. Added 15 more cases across 3 new concerns + 4 existing files, bringing the TUI suite to **50 cases across 9 files / 111 cases total across 15 files**. New: `agent-cycle.test.tsx` (Tab/Shift+Tab on open page cycles agents, locked once messages exist), `secondary-page.test.tsx` (ESC and Ctrl+[ both close the article-reader page). Extended: `autocomplete.test.tsx` with mention-insert-via-Enter + `/article @<path>` arg-expansion + slash-dropdown-visible-suspends-Ctrl+N; `streaming.test.tsx` with `update_sidebar` tool result → sidebar section renders; `session-list.test.tsx` with `[Interrupted by user]` dangling-marker rendering on resume of a session whose last agent_message was role:"user"; `prompt.test.tsx` with `/clear` mid-stream wiping messages; `dialogs.test.tsx` with DialogTheme + DialogModel open-and-ESC round-trips. Fake-session enhancement: `session.selectAgent` now mutates the fake's `agentName` reflection (not just recording the call) so `agentSession.agentName` reads pick up the swap — matches the real Session's mutate-in-place behavior. `bun run ci` green; `bun x tsc --noEmit` clean.

- [x] TUI test suite. Added `test/tui/` with a `renderApp()` harness that mounts the shipping provider stack (Theme → Toast → Dialog → Command → AgentProvider → Layout) around OpenTUI Solid's `testRender`, and a `makeFakeSession()` factory that captures `onEvent` so tests script synthetic `AgentEvent`s through the real reducer in `src/tui/context/agent.tsx` without a pi-agent-core loop. Seam: one new optional prop `session?: SessionFactory` on `AgentProvider` (defaults to the real `createSession` from `@backend/agent`); `Layout` is exported from `src/tui/app.tsx` so the harness can mount the same component `App` renders. No reducer extraction, no bridge changes, no provider reshuffling. Eight test files (~35 cases) covering: open page, conversation rendering (text / thinking / redacted-thinking / tool pending→completed / tool error / assistant error panel / streaming hint), streaming flow (tool-use turn, agent_end pending-tool sweep, sequential turns, duration footer), prompt submission (plain prompt, `/clear`, unknown slash fallthrough, streaming-gated submit, empty no-op), ESC double-tap interrupt, slash + mention autocomplete (list, filter, select-argless, select-argful, ESC, mention dismiss), command palette (open / ESC-close / select-entry), session list panel (Ctrl+N open, narrow-terminal toast, ESC close, Enter resumes). Assertions run against `captureCharFrame()` substrings; a `waitForFrame(needle)` helper polls renderOnce + captureCharFrame because markdown rendering goes through a tree-sitter worker and isn't synchronous. `bunfig.toml` `[test] preload` now includes `@opentui/solid/preload` alongside the existing `./test/preload.ts`.

- [x] Close the store/DB drift window on reducer mutation paths via persist-first ordering. Previously, three reducer branches in `tui/context/agent.tsx` mutated the Solid store *before* calling `runInTransaction(finalize…)` (plus two more found during implementation — `agent_end` duration stamp and `wrappedActions.prompt` user-bubble push), so a thrown SQLite write left the live view ahead of disk: bubble rendered `completed`/`error`/stamped-footer, `/resume` would regress it. Fix inverts ordering at 5 reducer sites: `message_end` (assistant commit), `tool_execution_end` (tool-state flip), `agent_end` (pending-tool sweep + duration stamp), and `wrappedActions.prompt` (user bubble). Each site builds the intended post-state as a plain object, writes it via `persistThen(writes, onSuccess)` — a new ~10-line helper in `agent.tsx` that runs `runInTransaction(writes)` and only applies `onSuccess()` if the tx committed. On failure, the toast already fired by `reportPersistenceError` is the only user-visible signal; the store stays at its pre-mutation value, so what's on screen is exactly what `/resume` would reconstruct. Invariant narrowed: pre-stream appends (new bubble / new shell / tool-result persist / synthesized-abort persist) keep today's order — there's no already-persisted state to regress. Supporting changes: (a) four writers (`appendDisplayMessage`, `updateDisplayMessageMeta`, `finalizeDisplayMessageParts`, `appendAgentMessage`) changed from report-and-swallow to report-and-rethrow via a new `tagReportedAndRethrow` helper that marks the error with a `REPORTED_SENTINEL` flag. (b) `runInTransaction`'s new outer catch reports pre-writer failures (db-acquire / SQLITE_BUSY / tx-open) as `action: "tx"` and reads the sentinel to dedup — one toast per failure, not two. Sentinel handling is scoped to this one call site so the flag doesn't leak onto config/auth error surfaces. (c) New `safeRun(fn)` helper in `sessions.ts` — 4 lines — wraps the 6 non-reducer persist sites that keep today's "log and continue" behavior (pre-stream appends, synthetic error bubble, synthesized-abort loop, `displayMessage` helper). Explicit rationale comments at the two correctness-adjacent sites: tool-result persist (`message_end` non-assistant branch) notes "persistence failure is benign at runtime but causes missing tool-result row on resume — out of scope for the drift fix"; synthesized-abort persist notes "failure absorbed by load-time alternation repair in sessions.ts — don't harden this into persistThen". New `test/persistence-failure.test.ts` (13 cases) covers: writer throw-propagation on FK violations, tx-scoped dedup sentinel semantics (bare-`reportPersistenceError` calls are NOT deduplicated — sentinel only lives where `runInTransaction`'s outer catch reads it), `persistThen` gating on single-writer and multi-writer tx rollback paths, pre-writer tx failure reported as `action: "tx"`, and `safeRun` swallow-vs-pass-through. No reducer factory extraction — tests exercise the pattern, not the full reducer integration (rejected as over-engineering). Docs: `SQL.md` writer-contract section updated with new throw-uniformly contract + `safeRun`/`persistThen` recipes; `ARCHITECTURE.md` reducer section gains the narrowed invariant sentence. `bun run ci` passes (62 tests); `tsc --noEmit` clean.

- [x] Compose `/article` with `@`-mentions + post-select coaching hint. Resolves the "user selects `/article` from the slash dropdown, `@`-picks a vault file, submits, reader throws `Article not found: @010 RAW/...`" bug. Two slices in one PR: **(A) Compose fix** — `prompt.tsx:handleSubmit` now reads mention extmarks once at the top (before the slash branch) and, inside the slash branch, expands mentions whose range falls inside the args slice to their absolute vault paths via a new pure helper `expandMentionsToPaths(text, mentions)` in `src/tui/util/mentions.ts`. Distinct from `buildMentionPayload` — the helper splices the resolved absolute path in place of the `@<path>` substring without reading file contents (slash args want a bare path, not a `Path: + Content:` block). Reader's `articleCommand` unchanged: `resolve(ARTICLES_DIR, absolutePath)` returns the absolute path per Node's documented behavior, and the existing `isInsideDir(articlePath, ARTICLES_DIR)` check rejects paths into Scraps / Notes / outside the vault. Defense-in-depth: mentions whose resolved path escapes `VAULT_DIR` (corrupted extmark metadata) keep their literal `@<path>` substring — the downstream command then fails on its own terms with a clearer error than "not in X folder." Mentions before the first whitespace (inside the verb `/name`) are filtered out of the args-expansion range and stay as literal text; `canRunSlash` fails to match the mangled name and the input falls through as a plain prompt (non-surprising fallback). Trailing text after a mention (e.g. `/article @foo.md extra words`) is passed through verbatim — reader's "Article not found" on the full trailing string is clear enough for a rare user-input bug, and an explicit comment in `handleSubmit` documents the choice so a future maintainer doesn't treat it as a bug. Slash-start anchor uses `rawText.indexOf("/")` instead of index 0 so buffers with leading whitespace (`  /article @foo.md`) dispatch correctly. **(B) Post-select coachi... (line truncated to 2000 chars)

- [x] Fix `/article` slash-dropdown selection for optional-arg commands. Selecting `/article` from the slash dropdown now inserts `/article ` into the textarea (letting the user optionally type a filename) instead of firing the recommendation picker immediately. Root cause: the dropdown's argful-vs-argless branch in `src/tui/components/prompt-autocomplete.tsx` keyed on `SlashSpec.takesArgs`, but `/article` sets `takesArgs: false` + `argHint: "[filename]"` — bare invocation is valid (opens the picker), arg is optional. `takesArgs` answers the narrower "is an arg REQUIRED for dispatch" question used by `canRunSlash`/`triggerSlash`; the dropdown UX wants the broader "does this command ACCEPT an arg (required or optional)" signal, which `argHint` presence already carries. Swapped the branch to `argHint`, no type or dispatch changes. JSDoc on both `SlashSpec.argHint` (`src/tui/components/dialog-command.tsx`) and `AgentCommand.argHint` (`src/backend/agent/types.ts`) now spells out the dual role (palette title + dropdown UX gate) so the implicit invariant is declared rather than tripwire-implicit. Deferred (per D8): the deeper three-way conflation on `takesArgs` (dispatch gate + palette visibility via `agent.tsx:1005` + dropdown UX) only has one affected command today and cleaning it up to a three-valued `args: "none" | "optional" | "required"` field is ~15 lines across 4 sites — revisit when a third command arrives.

- [x] Extract file-chip click handler out of `UserPart` into `src/tui/util/file-part-handler.ts`. `UserPart` is a generic bubble-part renderer; today's click body hard-coded `VAULT_DIR` + `isInsideDir` sandbox + `readFileSync`, dragging backend filesystem knowledge into a rendering component. New `openVaultFilePart(part)` owns the vault-resolve → sandbox-check → read → `openSecondaryPage` flow (byte-identical to the old inline body, including error branches for outside-vault and missing-file). `UserPart.handleClick` collapses to a one-line delegation; `user-part.tsx` drops its `node:fs` / `node:path` / `@backend/agent/constants` / `@backend/agent/permissions` / `openSecondaryPage` imports. Doc comment on the new util notes the vault-specificity is intentional for now and names the generalization trigger — "when a second producer emits file parts pointing at a non-vault source, revisit as a dispatch table keyed on a new `source` discriminator on `DisplayPart.file`". No registry built speculatively (only two producers exist today: reader's `/article` and `@`-mentions, both vault-scoped). No bridge-type changes, no schema changes, no producer changes, no behavior change — pure function extraction. Verification: `bun run check` + `bun run ci` green; `tsc --noEmit` clean. Manual smoke only for the click path (no automated test coverage of the click handler before or after — acceptable because the extracted body is byte-equivalent).

- [x] Consolidate keybind suppression in `CommandProvider`. Global keybind dispatch was previously gated by two independent guards inside `CommandProvider`'s `useKeyboard` (`src/tui/components/dialog-command.tsx`): `suspendCount > 0` (driven by the autocomplete dropdown) and `dialog.stack.length > 0` (dialog-open short-circuit). Collapsed into the single `suspendCount`-based gate. `DialogContext` grows a `setSuspendHandler(handler | null)` method; `CommandProvider` installs `{ suspend, resume }` at mount and unwires on cleanup. `DialogProvider`'s `replace()` calls `suspend()` on the empty→non-empty transition; `clear()` and the `dialog_close` handler call `resume()` on the non-empty→empty transition. Transition-edge counting (not per-dialog counting) keeps the count balanced even though today's stack holds at most one dialog — defends against future multi-dialog stacks. The `dialog.stack.length > 0` check is removed from the global `useKeyboard` in `dialog-command.tsx`. Other `dialog.stack.length` call sites (scroll gating in `app.tsx`, focus gating in `prompt.tsx`, the autocomplete dropdown's own keyboard handler in `prompt-autocomplete.tsx`, keyboard scoping in `dialog-select.tsx`) serve different purposes and are unchanged. Docstrings updated in `keybind.ts`, `dialog-command.tsx`, and `prompt.tsx` to reflect the unified gate. No behavior change. `bun run check` clean; `bun run ci` passes (biome + 49 tests); `tsc --noEmit` clean. Considered (and rejected) a provider-tree inversion (`CommandProvider > DialogProvider`) during planning — cleaner dependency graph but dropped as over-engineering for a "small cleanup; no behavior change" task per the behavioral-guidelines review.

- [x] `/clear` mid-stream no longer dirties pi-agent-core state. `Session.clearSession()` previously did `agent.state.messages = []` and nothing else, leaking `activeRun` + `isStreaming` + `streamingMessage` + `pendingToolCalls` when invoked during a live turn. The next `prompt()` threw `"Agent is already processing a prompt."` (pi-agent-core `agent.js:216-217`), surfaced via `wrappedActions.prompt`'s pre-stream catch as an `"Agent error"` toast. Fix uses pi-agent-core's own primitives: when `agent.state.isStreaming || agent.signal`, call `agent.abort()` (cancels `activeRun`, fires a final `message_end` with `stopReason: "aborted"` that the reducer already surfaces), `await agent.waitForIdle()` (blocks until `agent_end` listeners settle so no late event lands on a wiped Agent), then `agent.reset()` (pi-agent-core's authored "clear transcript + runtime fields + steering/follow-up queues" primitive, `agent.js:206-214`). Idle-session path: `signal` is undefined, `waitForIdle()` returns `Promise.resolve()`, `reset()` still runs but is cheap — one extra microtask vs the old single-line assignment. `Session.clearSession` signature flipped `void → Promise<void>`; TUI `wrappedActions.clearSession` became `async` and awaits the backend clear *before* wiping store fields — mid-stream ordering matters because `abort()`'s final `message_end` fires through the reducer (writing `isStreaming = false` + sweeping pending tool parts into error state) and a wiped store would drop those writes. Two non-awaited call sites: (a) `resumeSession`'s `batch()` calls `void agentSession.clearSession()` — the function is pre-guarded on `!store.isStreaming`, so the idle short-circuit makes this safe to fire-and-forget; (b) `app.tsx`'s `/clear` palette entry calls `void actions.clearSession()` — command `onSelect` returns `void`, and the Promise can't reject (pi-agent-core's `reset()` is synchronous internally, `waitForIdle` resolves). `bun run ci` passes (biome + 49 tests); `tsc --noEmit` clean.


- [x] `@` file references in the prompt. Typing `@` after whitespace or at start-of-input opens a fuzzy-filterable dropdown of vault files (`.md` / `.markdown` / `.txt`); selecting a file inserts a highlighted `@path` span into the textarea via an OpenTUI virtual extmark (styled via the `extmark.file` scope in `getSyntaxRules` — `theme.warning` + bold; registered inside the shared `SyntaxStyle` so the id resolves against whichever instance is live after a theme switch). On submit, the user bubble renders `[md] path` / `[txt] path` chips interleaved with literal text (reusing the `file` `DisplayPart` chip shipped with `/article`); pi-agent-core receives the prompt text with each successfully-read `@path` replaced by a `Path: <path>\n\nContent:\n\n<body>` block — same format as reader's `/article` so the LLM sees one convention for "file inlined into a user prompt." Failed reads (missing / symlink / outside vault / disallowed extension / I/O error) degrade to literal `@path` in both bubble and LLM text with a single `"Could not read N file(s)"` toast. **Prompt is an uncontrolled `<textarea>`** (`minHeight=1`, `maxHeight=6`, `wrapMode="word"`) — not `<input>`, so long prompts wrap to additional lines instead of horizontally scrolling. No `value=` prop: `onContentChange` mirrors `plainText` into `text()`. Writes go through the renderable imperatively — slash selection calls `input.setText(...)`; mention selection calls `input.deleteRange` + `input.insertText` + `input.extmarks.create({ metadata: { path } })`. Going uncontrolled was the pivot that fixed typing after extmarks were introduced: a controlled `value={text()}` write-path round-trips through `InputRenderable.set value` → `setText` → `wrapSetText` which clears all extmarks on every reactive update. Textarea keybindings (mirrors OpenCode's `input_submit` / `input_newline`): `Enter` submits; `Shift+Enter` / `Ctrl+Enter` / `Alt+Enter` / `Ctrl+J` insert a newline. Slash + mention modes are mutually exclusive via a single `mode: "slash" | "mention" | null` state machine; slash wins while active. Vault file list: synchronous recursive walker with session-scoped module-local cache (`listVaultFiles()` — first `@` trigger walks, subsequent hit the cache); skips leading-dot entries, `node_modules/`, and symlinks. `ALLOWED_VAULT_EXTENSIONS` is exported and re-applied inside `readFileSafe` so a manually-typed `@foo.json` can't bypass the dropdown's filter. `invalidateVaultFileCache()` exported but unused — reserved for a future refresh command or `fs.watch` hook. New files: `src/tui/util/vault-files.ts`, `src/tui/util/mentions.ts` (pure `buildMentionPayload` + `readFileSafe`), `test/mentions.test.ts` (10 cases covering empty / single / multi / adjacent / mixed success-failure / trailing mention / all-failed / mime resolution / unsorted-input contract). Trimmed from OpenCode's equivalent: frecency, directory expansion, line ranges, `@agent` sub-agent routing, mouse handling, `input.traits.capture` (kept existing `useKeyboard` dispatch). No persistence schema changes — `parts` table's `mime` / `filename` columns from `/article` round-trip mentions on resume. Secondary fixes shipped together: `flexGrow={1}` on prompt + user-bubble boxes so backgrounds fill the row, `syntaxStyle={syntax()}` on the prompt textarea so extmark `styleId`s have a palette to resolve against, `text/plain` → `txt` badge mapping in `user-part.tsx`. CodeRabbit follow-ups addressed: `filterText` bounded by `input.cursorOffset` (fixes mid-buffer mentions being swallowed by trailing suffix); nav handler (Up/Down/Ctrl+N/Ctrl+P) no longer consumes keys when `filtered().length === 0`. Two pre-impl reviews + one post-impl review by fresh-context subagents; all actionable findings addressed. `bun run ci` passes (biome + 49 tests).

- [x] Thorough codebase audit for feature/doc mismatches, bugs, and security issues. Review-only pass across docs, backend agent/persistence/permissions/providers, TUI reducer/commands/rendering, tests, dependency surface, and git hygiene. `bun run ci` passes (Biome + 39 tests); `bun x tsc --noEmit` passes. `bun pm scan` is not configured; `bun audit` reports dependency advisories logged in Known Issues below. Findings were not fixed in this pass.

- [x] Tool-call rendering polish: drop result-summary line; end turn after `update_sidebar`. Follow-up to the tool-call rendering feature below. Two changes: (1) `ToolPart` no longer renders the second muted line with the result summary — for today's tools (`read`/`edit`/`write`/`update_sidebar`) the result text (`"Sidebar section 'x' updated."`, `"Read <path>"` etc.) is redundant with the args already in the header. Removed `summarizeToolResult` from `tui/context/agent.tsx`, the `result` field from `DisplayPart.tool`, from the `tool_data` JSON `$type<>` on the `parts` table (schema SQL unchanged — the field was only in the TS hint for the existing JSON blob), and from `serializePart`/`deserializePart`. Errors still render a second red line — failure info isn't in the args. If a future tool's result carries info the args don't (e.g. `grep` match count), reintroduce a summary path. (2) `updateSidebarTool` sets `terminate: true` on its `AgentToolResult`; pi-agent-core's `shouldTerminateToolBatch` stops the run after the batch when every tool in the batch sets the flag. Eliminates the wasted ~100-token follow-up assistant turn after Stage 2 (previously the LLM emitted a closing "Ready when you are" ping after seeing the tool result). Reader's Stage 2 instructions updated to note the tool ends the turn — belt-and-suspenders so a future maintainer reading the instructions sees the intent even if the `terminate` flag is unclear. `bun run ci` passes (biome + 39 tests); `tsc --noEmit` clean.

- [x] Display tool calls inline on the originating assistant bubble. Resolves the "second assistant bubble with no visual indication of the tool invocation between them" gap. `DisplayPart` gains a `tool` variant `{ callId, name, args, state, result?, error? }`. The `message_update` reducer in `tui/context/agent.tsx` handles pi-ai's `toolcall_end` (pushes `state: "pending"` onto the assistant bubble that emitted the call — same bubble pi-ai put the `toolCall` block on in its `content` array) and `tool_execution_end` (scans tail-first across recent assistant bubbles to locate the matching `callId`, flips to `"completed"` / `"error"`, and re-runs `finalizeDisplayMessageParts` in a transaction so the DB matches live state). The existing `update_sidebar` sidebar-mutation path stays in `tool_execution_end` as an orthogonal side effect. New `ToolPart` component in `message.tsx` renders a single muted line (`⚙ name args`) — `~` icon while pending, `⚙` when completed, red strikethrough on error — with an optional short result summary beneath. Args formatter in `tui/util/tool-summary.ts` has per-tool cases for `read`, `write`, `edit` (path + edit count), `update_sidebar` (operation + id), and a JSON fallback for unknown tools. Persistence: `parts.type` enum widens to include `"tool"`; two new columns `call_id TEXT` and `tool_data TEXT` (JSON blob holding `{ name, args, state, result?, error? }`) added. `serializePart` / `deserializePart` grow a `tool` branch mirroring `file`. Pre-1.0 nuke mandate applied — `0000_init.sql` + snapshot regenerated from schema; dev DBs deleted by hand. Docs: `ARCHITECTURE.md` file-structure + Bubble-per-assistant-boundary sections updated; `SQL.md` parts-table column description rewritten to cover the new fields. `bun run ci` passes (biome + 39 tests); `tsc --noEmit` clean.

- [x] Article reader page + sidebar sections tool. Clicking the `md` file chip in a user bubble opens a full-screen article reader page (replaces the conversation area). The page renders the article markdown from disk via `<markdown>` in a scrollable view. The sidebar gains dynamic sections driven by a new `update_sidebar` tool (generic, available to all agents via `BASE_TOOLS`): agents call it with `{ operation: "upsert"|"delete", id, title, content }` to add/update/remove sidebar sections. The reader agent's Stage 2 instructions now tell the LLM to call `update_sidebar` with the first-pass prompts after generating them, so they appear in the sidebar. Navigation: Ctrl+[ or a "← Back" button in the sidebar returns to the conversation. New store fields: `sidebarSections: SidebarSection[]` (ephemeral, cleared on `clearSession`/`resumeSession`), `articleView: { filename } | null`. New actions: `openArticle(filename)`, `closeArticle()`. The article page includes an `isInsideDir` sandbox check to prevent path traversal. New keybind: `article_close: "ctrl+["`. Files: `view-model.ts` (types), `tools.ts` (tool), `compose.ts` (BASE_TOOLS), `agent.tsx` (reducer + actions + store), `user-part.tsx` (clickable chip), `article-page.tsx` (new), `sidebar.tsx` (dynamic sections + back button), `app.tsx` (routing + keybind), `keybind.ts` (new action), `instructions.ts` (Stage 2 update_sidebar call). `bun run ci` passes (biome + 39 tests); `tsc --noEmit` clean.

- [x] Doc cleanup — strip inline code references from ARCHITECTURE.md, AGENT-DESIGN.md, SQL.md. Replaced inlined TS interfaces/shapes (AgentStoreState, DisplayMessage, ProviderInfo, CommandOption, AgentCommand, config.json effort keys, reader permissions overlay) with prose summaries + file-path pointers. Trimmed the 70-line annotated file tree in ARCHITECTURE.md to one-line-purpose annotations. Stripped OpenCode line-range citations (e.g. `routes/session/index.tsx:1437-1468`) down to bare file paths. Replaced `node_modules/` dist paths with package-name references. Removed the duplicated error-surface ts block in SQL.md. Kept: Provider Stack nesting tree, transaction-example ts blocks in SQL.md, sh/sql recipe blocks. All `src/` paths in the cleaned docs verified to resolve. No code changes.

- [x] Slash-command dropdown in the prompt textarea. Typing `/` at column 0 opens a dropdown above the textarea listing all registered commands with a `slash` field, filtered via `fuzzysort`. Keyboard: Up/Down (+ Ctrl+P/Ctrl+N) navigate, Enter/Tab select, Esc dismiss. When the dropdown has matches, Enter selects; when empty, Enter falls through to the existing `handleSubmit` path (plain prompt or `triggerSlash`). Argless commands fire immediately; argful commands insert `/name ` and close the dropdown. Dismisses on Esc, space, backspace past `/`, or selection. New `src/tui/components/prompt-autocomplete.tsx`; `prompt.tsx` gains `position="relative"` on the outer box and mounts `<PromptAutocomplete>`. `dialog-command.tsx` gains `suspend()`/`resume()` to block global keybind dispatch while the dropdown is visible. Ported from OpenCode's `prompt/autocomplete.tsx` (slash subset only). `bun run ci` passes (biome + 39 tests).

- [x] Render `/article` with a compact user bubble (short prose + file chip). Today reader inlined the full article body as the user's prompt text, so the bubble for a 10 KB article buried the rest of the conversation. Ported OpenCode's split of "one representation, two renderers": `DisplayPart` grew a `file` variant (`mime`, `filename`, display-only — no url/content); the user bubble iterates parts and paints a `[md] 010 RAW/013 Articles/foo.md` chip row for each `file` part via a MIME-badge lookup (markdown only today; unknown mimes fall through to the raw mime string). `AgentCommandHelpers.prompt` widened from `(text) => Promise<void>` to `(text, displayParts?) => Promise<void>`: when `displayParts` is supplied, `wrappedActions.prompt` in `tui/context/agent.tsx` places those parts on the user `DisplayMessage` verbatim while pi-agent-core still receives only `text` — the LLM sees the full `Path: + Content:` body as before, the bubble renders whatever the command asked for. Reader's `runArticle` helper (shared by the picker-bare-case and the direct-filename case) passes `[text "Read this article.", file text/markdown <vault-relative>]` where the filename comes from `path.relative(VAULT_DIR, articlePath)`. Persistence: added two nullable TEXT columns (`mime`, `filename`) to the `parts` table (flat over JSON so `listSessions`'s preview fallback can read `filename` in SQL without JSON extraction); `serializePart`/`deserializePart` centralize the mapping so both `appendDisplayMessage` (message_start) and `finalizeDisplayMessageParts` (message_end) produce identical rows. `listSessions` preview falls back to the first file part's filename when no text parts survive — a `/article`-only session shows the vault path in the session list instead of an empty row. Corrupt file rows (missing mime/filename under current writers shouldn't happen, but defense-in-depth) degrade to an empty text part AND report through `reportPersistenceError` so silent corruption surfaces as a toast. Sidebar title uses a first-part-wins rule (text → flat body, file → tail-truncated filename) so the label matches what the bubble visually emphasizes. Non-first parts in the user bubble get `marginTop={1}` to match `AssistantMessage`'s parts-loop stacking. Reducer's `text_delta`/`thinking_delta` store mutation refactored from the path-based `setStore("messages", i, "parts", j, "text", fn)` to `produce(p => if p.type === "text" || p.type === "thinking") p.text += ...)` so the union widening doesn't break the path typing. Pre-1.0 nuke mandate — migration regenerated in place via `bunx drizzle-kit generate --name init`; `0000_init.sql` is the fresh init; dev DBs deleted manually. Docs: `bridge/view-model.ts` docstring updates on `DisplayPart` + `DisplayMessage.parts`; `docs/ARCHITECTURE.md` `DisplayMessage` block + `DisplayPart` union + `AgentCommand` shape + slash-dispatch diagram + Agents-on-ship reader row; `docs/SQL.md` `parts` row. New test file `test/display-file-part.test.ts`: 3 cases (round-trip hydrate, preview fallback to filename, preview prefers text when both). Integrated atop the bare-`/article` picker by routing `runArticle` through the widened prompt for both code paths (picker selection → `runArticle(picked, helpers.prompt)`, direct-filename → `runArticle(filename, helpers.prompt)`).

- [x] Bare `/article` recommendation picker. When the user types `/article` with no argument (or whitespace-only), the reader agent scans `ARTICLES_DIR`, scores unread articles using the `index.base` ranking logic (reading_score = unread bonus + saved_band + published_band; sort by score DESC, published DESC, filename ASC), pushes a numbered recommendation list into the conversation as a persisted user bubble, then opens a `DialogSelect` picker. Selecting an article runs the existing `/article <filename>` path (load file → start reading workflow). Cancelling (ESC) leaves the list in the conversation without starting a turn. New `AgentCommandHelpers` type widens `AgentCommand.execute`'s second arg from a bare `prompt` function to `{ prompt, displayMessage?, pickFromList? }` — generic primitives any future agent command can use. `takesArgs` flipped from `true` to `false` so the bare invocation is dispatched by the slash gate; `/article` now also appears in the Ctrl+P palette (clicking it opens the picker). New `src/backend/agent/agents/reader/recommendations.ts` owns the ranking logic (faithful port of `index.base` formulas: saved_band from fs mtime, published_band from frontmatter, reading_bucket, reading_score). Minimal frontmatter parser handles `key: value` lines with optional quotes — sufficient for `title`, `published`, `description`, `reading_completed`. 17 new tests (scoring helpers, recommendArticles, bare-case command flow, picker selection, whitespace-only args). `bun run ci` passes (biome + 35 tests); `tsc --noEmit` clean.

- [x] Ctrl+N session panel row redesign + cross-agent listing and resume. Row layout is now two lines: line 1 is `● <title>` (unchanged); line 2 is `<agent> · <relativeTime>` with the agent token tinted via `theme[getAgentInfo(row.agent).colorKey]` so cross-agent scanning is visual. Dropped `messageCount` from the row — redundant with the sidebar on the active session and non-load-bearing for picking a row. `listSessions(agent)` → `listSessions()`: the agent filter is gone; every session in the store is listed newest-first, each `SessionSummary` already carried `agent` so no shape change. `resumeSession` in `tui/context/agent.tsx` deletes the old agent-mismatch toast outright and allows cross-agent resume silently. New ordering inside the resume `batch()`: `agentSession.clearSession()` → conditional `agentSession.selectAgent(loaded.session.agent)` when the target session's agent differs from the live one → `agentSession.restoreMessages(loaded.agentMessages)` → `setStore("currentAgent", agentSession.agentName)` + `messages` + counters. Ordering is load-bearing: `Session.selectAgent` throws when the live Agent's `messages.length > 0`, so the clear must precede it; `restoreMessages` must follow so the seeded history isn't wiped by the clear. The in-batch `currentAgent` write drives prompt accent color, sidebar header, and `CommandProvider`'s agent-scoped registrations to re-derive against the resumed agent on the next tick. D13 in `docs/AGENT-DESIGN.md` tightened to "one agent per session for the session's **in-memory** lifetime" — persisted sessions are replayable into any agent context because resume constructs a fresh in-memory lifetime bound to the stored session's agent; the invariant is preserved, not broken. Docs: `docs/SQL.md` §Visibility rewrite (replaces the `listSessions(agent)` / "every read path filters `WHERE sessions.agent = ?`" paragraph), `docs/ARCHITECTURE.md` §"Session list panel (Ctrl+N)" rewrite (new row description, new resume ordering, cross-agent rationale), `docs/AGENT-DESIGN.md` D13 phrasing update. `bun run ci` passes (biome + 24 tests); `tsc --noEmit` clean.

- [x] Fix `[user, user]` corruption on resume via load-time tail repair. Reproducer: user typed `:q`, pressed Enter, then hit Ctrl+C before the assistant's `message_end` could commit. The user `AgentMessage` for `:q` was written to `agent_messages` (pi-agent-core emits `message_end` for user messages *before* the assistant stream begins) but no closing assistant row ever landed. Weeks later, resume via Ctrl+N + a new prompt `test` handed the provider `[user:":q", user:"test"]`. Anthropic's Messages API silently merges consecutive same-role turns (documented at docs.claude.com/en/api/messages), so the model saw one user turn containing `:qtest` and replied referencing that string. Bedrock would have returned a `ValidationException: Conversation roles must alternate` 400 instead — same class of bug, different surface. Fix: in `loadSession`, after reading `agent_messages`, inspect the tail; if `role === "user"`, append a synthetic `assistant` `AgentMessage` with `stopReason: "aborted"`, `errorMessage: "[Interrupted by user]"`, empty `content`, and `api`/`provider`/`model` copied from the latest prior assistant in the same session (falling back to bland defaults `anthropic-messages` / `amazon-bedrock` / `placeholder` when the session was interrupted on its very first turn — the placeholder never reaches a provider, so dummy strings are safe). Pure read-time repair: stored rows are never mutated, so the DB stays auditable. Display layer gets an `isDanglingUser` memo in `conversation.tsx` that renders a muted `[Interrupted by user]` marker beneath any user bubble with no assistant following and no in-flight stream pending — mirrors the backend repair so the resumed transcript reads correctly. Researched against Claude Code (repair-at-load via `NO_RESPONSE_REQUESTED` sentinel + prevention via `createUserInterruptionMessage`), OpenCode (eager-write prevention via pre-allocated assistant row + read-time content-filter), pi-agent-core (prevention-only via `handleRunFailure`), and pi-mono coding-agent (relies entirely on pi-agent-core). Anthropic Messages API merges consecutive same-role turns silently; Bedrock Converse 400s; OpenAI Chat Completions accepts as-is — documented in the task research. Picked Claude Code's load-time pattern trimmed to Inkstone's scope because: (a) it covers the process-kill case pi-agent-core's prevention can't reach (hard exit bypasses `handleRunFailure`), (b) the bug we hit is the primary concern here (narrowly scoped fix), and (c) eager-write prevention (OpenCode's Layer 1) is a larger refactor deferred to future work. 5 new tests in `test/resume-repair.test.ts` covering dangling tail, clean tail, dangling tail with prior assistant (metadata inheritance), empty session, and first-turn-interrupt (dummy-default fallback). Verified: `bun run ci` passes (biome + 23 tests); `bun x tsc --noEmit` clean.

- [x] `listSessions` preview query — pre-filter first user message in SQL. The previous query joined `messages ⋈ parts` across every user message of the visible sessions and then locked in the first-seen messageId per session via a JS loop, relying on UUIDv7 lexical ordering = chronological. Replaced with a `GROUP BY session_id / min(messages.id)` subquery joined onto `parts`, so the row count out of SQL is proportional to (sessions × parts-of-first-user-message) instead of (total-user-messages × parts). Same invariant backs correctness — `min(id)` = earliest — already documented in `docs/SQL.md` §Identity model, so no doc change needed. Also corrected a stale inline comment that said "first text part" when the code has always concatenated all text-typed parts of that message. Behavior unchanged; `firstUserMsgBy` tie-break map deleted since the SQL now guarantees one messageId per session in the result. `bun run ci` passes (biome + 18 tests); `tsc --noEmit` clean. Post-implementation review by fresh-context subagent returned zero actionable findings (all info/nit, no blockers or should-fix).

- [x] Session list panel — Ctrl+N toggles a left-side panel listing past sessions for the current agent; selecting a row resumes that session in place (messages reload, Agent state seeded via `Session.restoreMessages`, usage counters reset). New keybinds: `session_list: "ctrl+n"` and `panel_close: "escape,ctrl+n"` (ESC and Ctrl+N close the panel symmetrically). `ctrl+n` collides with `select_down`'s alternate inside dialogs — resolved by CommandProvider's `dialog.stack.length > 0` short-circuit (same guard that protects `ctrl+p` / `command_list`) and by the panel's own `useKeyboard` checking `panel_close` before `select_down` so a reopen-key-as-close always wins. Backend adds `Session.restoreMessages(messages: AgentMessage[])` — minimal accessor for the load path since `agent` is private to the `createSession` closure; name signals load-path-only intent so callers don't reach for it mid-turn. `AgentProvider` gains a `resumeSession(sessionId)` wrapped action: guards on `store.isStreaming` with a toast ("Press Esc to stop the current turn, then try again"), calls `loadSession`, toasts on agent-mismatch instead of throwing (reachable via Tab-cycle-while-panel-open, not only invariant violations), then batches `clearSession` + `restoreMessages` + store reset inside a `batch()`. `listSessions` query now also computes a single-line `preview` from each session's first user message (join `messages + parts WHERE role='user'`, ORDER BY message.id ASC, lock in first message per session in JS — relies on UUIDv7's global time-monotonicity) so panels without titles show meaningful labels instead of "Untitled". New `SessionList` component (~240 lines) reuses the DialogSelect visual pattern (●-indicator for current session, primary-bg highlight, truncation) but with a purpose-built keyboard handler; `onMount` blurs the prompt input so Enter/arrows don't double-dispatch through both surfaces, paired with `refocusInput()` on close. Click-to-select fires `onSelect` immediately (mouseUp), matching DialogSelect. Layout in `app.tsx` registers `session_list` as a hidden keybind-only command that toggles the panel; guards opening on `dimensions().width < 80` with a toast. When the panel is open, the right metadata Sidebar is auto-hidden (single width-threshold rule replaces the dual "80 to open / 100 to hide sidebar" design from the review). `formatRelativeTime(ts)` added to `util/format.ts` ("just now" / "Nm" / "Nh" / "yesterday" / "MMM D"). Right-sidebar's `hasUsageData` memo already zeros the usage block on a loaded session, so resumed bubbles don't misreport "0 spent" next to N prior turns. Scope explicitly excluded: delete/rename, search/filter, cross-agent browsing, auto-titling, persisted panel open/close state. Plan reviewed twice by behavioral-guidelines + post-implementation fresh-context review that caught three real issues (keyboard double-dispatch, throw-vs-toast on agent mismatch, misleading `createMemo` on a non-signal) — all fixed. Verified: `bun run ci` passes (biome + 18 tests); `bun x tsc --noEmit` clean.

- [x] Move `clearSession` from `AgentActions` to `Session`. `clearSession()` is a session-lifecycle operation (same category as `selectAgent`), not a per-turn action (like `prompt`, `abort`, `setModel`, `setThinkingLevel`). Moved it from the `AgentActions` interface to `Session`, next to `selectAgent`, so the ownership split is consistent: `AgentActions` owns per-turn operations, `Session` owns lifecycle + accessors. One call-site change in `tui/context/agent.tsx` (`agentSession.actions.clearSession()` → `agentSession.clearSession()`). `AgentContextValue.actions` type widened to include `clearSession` alongside the existing `selectAgent` addition. No behavior change. Docs updated: `ARCHITECTURE.md`, `AGENT-DESIGN.md`, `TODO.md`.

- [x] Session-handle refactor + drop auto-resume + drop `ended_at`. Supersedes and extends the prior "drop per-turn `systemPrompt` rebuild" entry. Encodes "one agent per session" in the code structure rather than UI gates: `createSession({ agentName, onEvent }): Session` factory replaces the `getAgent` + `createAgentActions` + module-level singletons (`agent`, `currentProviderId`, `currentModelId`, `currentAgent`, `thinkingLevels`) surface. `Session` is a small façade over a pi-agent-core `Agent` bound to one agent name for its lifetime, exposing `actions`, `agentName`, `messageCount`, `getModel/ProviderId/ModelId/ThinkingLevel`, and a gated `selectAgent(name)` that throws when `messages.length > 0`. `AgentActions` narrows — `setAgent` is gone; empty-session agent pick now flows through `Session.selectAgent` (TUI wraps with a duplicate store-side guard). Boot always shows openpage: the `findActiveSession` / `repairSession` / `loadSession` + message-seeding block in `AgentProvider` is gone; session rows linger on disk for a future `/resume` command. `ended_at` column dropped inline via `schema.ts`, `0000_init.sql`, `0000_snapshot.json` edits (pre-1.0 nuke mandate — no migration, dev DBs deleted manually). `sessions.ts` loses `findActiveSession`, `endSession`, `repairSession`, the `SessionRecord.endedAt` / `SessionSummary.endedAt` fields, and every internal `ended_at` read; keeps `loadSession` + `listSessions` for the future `/resume`. `clearSession` wrapper in TUI becomes in-memory-only (no DB write). `app.tsx` dialog-seeding switches from module-level `getCurrent*` to an `AgentContextValue.session` accessor bag bridged into the context; `dialog-agent.tsx` + the Tab/Shift+Tab handler migrate to `selectAgent`. Docs: `SQL.md` column table, lifecycle diagram, public-API section, source-map, "Gotchas" entry for deferred crash-repair; `AGENT-DESIGN.md` D9's stability-invariant paragraph rewritten in terms of `createSession` + `selectAgent`, new `D13 — Session-agent binding` block citing cache stability, bubble provenance, and permission-model clarity as rationale; `ARCHITECTURE.md` System-prompt invariant paragraph, `/article` slash dispatch annotation, Switching rules diagram, and persistence summary all rewritten. `test/permissions.test.ts` unchanged (exercises dispatcher + reader command directly, not the agent shell). `bun run ci` green (biome + 18 tests); `tsc --noEmit` clean. No runtime behavior change visible to the user except (a) no auto-resume at boot, (b) mid-session agent swap throws instead of being UI-gated.

- [x] Drop per-turn `systemPrompt` rebuild in `backend/agent/index.ts`. The `prompt(text)` wrapper ran `a.state.systemPrompt = composeSystemPrompt(getAgentInfo(currentAgent))` on every turn as a "forward-compatible invariant" for future agents that mutate `buildInstructions`-visible state. Surveyed five references (`~/dev/claude-code`, `~/dev/opencode`, `~/dev/hermes-agent`, `~/dev/openclaw`, `~/dev/pi-mono`): pi-mono's own reference consumer `coding-agent` builds `_baseSystemPrompt` once and reassigns only on discrete events (tool-set change, resource reload, extension override) — rebuilding per turn violates the framework's expected usage pattern, because both Anthropic's `cache_control` block and Bedrock's `cachePoint` are pinned to the byte-exact system prefix. Today's rebuild was a no-op (reader + example have pure `buildInstructions`), so it was free in tokens, but invited the next agent author to read mutable state there and silently bust the prefix cache on every turn. Replaced with a "build-once + refresh-on-discrete-events" contract: `getAgent()` builds on construction, `setAgent()` rebuilds on swap, `clearSession()` rebuilds after history wipe — all three already existed. Dropped the per-turn rebuild; replaced the 8-line comment with a short invariant note pointing at `createContextSnapshot`, the two provider cache breakpoints, and the "dynamic context goes in user messages" rule (reader's `/article` as the reference pattern). Docs: `AGENT-DESIGN.md` D9 grows a "System-prompt stability invariant" paragraph citing pi-mono + the four other codebases for authority; `ARCHITECTURE.md` Commands section's per-command paragraph + the `/article` slash dispatch diagram annotation both restate the invariant so a future reader can't miss it. No behavior change today (both current agents pure). No type changes, no API changes. `bun run ci` passes (biome + 18 tests); `tsc --noEmit` clean. Added a Post-Implementation Review Protocol to `AGENTS.md` so every non-trivial change spawns a fresh-context review subagent going forward.

- [x] Nuke legacy `session.json` importer and related pre-SQLite surface. Deleted `src/backend/persistence/import-legacy.ts`, its single call site in `src/tui/context/agent.tsx`, the `SESSION_FILE` export from `paths.ts`, the `SessionData` interface from `@bridge/view-model`, the `"db"` value from `reportPersistenceError`'s `kind` union (collapsed the toast `titleKind` branch from four arms to three), and three stale "legacy / pre-transaction / backward compat" comments in `view-model.ts`, `schema.ts`, `sessions.ts`, and `errors.ts`. Docs: dropped the `session.json.migrated` row from `docs/SQL.md`'s file-layout table, removed `importLegacySessionJsonIfNeeded` from the session-lifecycle diagram, deleted the `import-legacy.ts` source-map row; dropped `SessionData` + `SESSION_FILE` + the `import-legacy.ts` line from `docs/ARCHITECTURE.md`'s bridge/paths/tree sections; dropped the "Older messages that predate these fields" footer sentence and the "pre-transaction data" backstop clause. Pre-1.0 nuke mandate — on-disk `~/.local/state/inkstone/session.json*` artifacts on dev machines can be deleted manually; nothing references them anymore. `bun run ci` passes (biome + 18 tests).

- [x] Reader-permissions test suite. Ported the hand-rolled smoke harness (`/tmp/inkstone-phase1/smoke4.ts`) into the repo as `test/permissions.test.ts` using `bun:test`. 18 cases: 11 permission-dispatch cases (parameterized via `test.each`: read/write/edit against Articles/Notes/Scraps/unzoned/outside vault, prefix-attack siblings) + 1 command-declared sanity check + 6 `/article` command cases (content load, missing, escape, empty, whitespace, symlink reject). `test/preload.ts` seeds an isolated tmp `XDG_CONFIG_HOME` + vault with `010 RAW/013 Articles/` populated (foo.md + bar.md + sneak.md symlink), wired via `bunfig.toml` `[test] preload = ["./test/preload.ts"]` — needed because `constants.ts` captures `VAULT_DIR` at module-eval via `loadConfig()`, so the env setup has to happen before any test file's static imports resolve. `package.json` gains a `test` script and `ci` bundles `biome check` + `bun test` so the full verification runs under one command. Verified: all 18 cases pass; hermetic (run twice → identical summaries); negative control (break `blockInsideDirs` → 2 cases correctly fail); `tsc --noEmit` clean. Follows OpenCode's `test/` + `bunfig.toml` preload pattern.

- [x] Reader statelessness refactor. Dropped `activeArticle` module state from reader; `/article <filename>` now resolves inside `ARTICLES_DIR` only, reads the file at invocation (throws if not found or if the resolved path escapes the Articles folder), and sends path + content as the opening user message. The file content travels with the conversation; no cross-turn state survives. Dropped `AgentCommandContext` — `AgentCommand.execute(args, prompt)` takes a positional `prompt` function, no context object. Reader's `getPermissions` becomes static on the Articles zone: `blockInsideDirs` on `write` (any article overwrite blocked) and `frontmatterOnlyInDirs` on `edit` (any article edit must target frontmatter). Added two new `Rule` kinds — `blockInsideDirs` and `frontmatterOnlyInDirs` — that generalize the old single-path variants (`blockPath` + `frontmatterOnlyFor`, both deleted) from "this specific file" to "any file under these directories." Removed: `activeArticle` from `AgentStoreState`, `SessionData`, the sessions DB column (schema updated; `0000_init.sql` regenerated from the current schema — pre-1.0, no user migration path, dev DBs are nuked on upgrade), the sidebar's Article panel, `setActiveArticle` shell re-exports, `getActiveArticle`, `persistActiveArticle` in the TUI bridge. **Behavioral shift** (see Known Issues): the Articles zone is now uniformly read-only-except-frontmatter for reader — previously only the currently-active article was restricted; other articles the agent had opened via `read` were edit-free. The new shape protects the whole Articles folder as a class. Acceptable for reader's workflow (the LLM rarely edits a non-focused article on purpose); if a reader session ever needs to bulk-edit historical articles, that's a separate tool surface. Reader's instructions updated: drops the "use `read` tool to load the article" section, clarifies the article arrives as the opening user message, removes the "Active Article" header + today's-date interpolation (date stays derivable from the LLM's own context).

- [x] Agent architecture refactor — zones as first-class workspace. Added `AgentZone { path, write: "auto" | "confirm" }` + required `zones: AgentZone[]` field on `AgentInfo` (`src/backend/agent/base.ts`). New `composeZonesOverlay(info)` derives permission rules from zones: `confirm` → `confirmDirs` (one rule combining all confirm zones); `auto` → nothing (baseline allows). Zone paths join `VAULT_DIR` via `node:path.join` so leading/trailing slashes normalize; `composeZonesOverlay` throws on absolute paths (POSIX `/`, Windows drive-letter via `path.isAbsolute`, UNC) and paths containing `..` segments (split on both `/` and `\` separators so Windows-authored `"..\\etc"` is rejected). New `composeOverlay(info) = info.getPermissions?.() ⊕ composeZonesOverlay(info)` — custom first, zones second, so the dispatcher's first-block-wins evaluation short-circuits on file-level custom rules before directory-level zone confirms fire; `beforeToolCall` in `backend/agent/index.ts` calls `composeOverlay`. Reader declares three zones (`010 RAW/013 Articles`, `020 HUMAN/022 Scraps`, `020 HUMAN/023 Notes`, all `confirm`); reader's `getPermissions` shrinks to just the `frontmatterOnlyFor` rule + the active-article `blockPath` for `write`. Example agent declares `zones: []`. `composeSystemPrompt` emits a `<your workspace>` block derived from `zones` when non-empty; policy verbs render as `"write freely"` / `"confirm before write"`. Tool baselines (`tools.ts`) trimmed to `insideDirs: [VAULT_DIR]` only — directory-level `confirmDirs` moved entirely to zones so the baseline + zone don't double-confirm writes to the same dir. `confirmDirs` dispatcher now fails closed when `confirmFn` is unset (pre-D12 it passed through as "safe because insideDirs already checked" — that justification weakens once zones own the policy, so headless callers must set an explicit confirm function). Prefix-boundary bug fixed in `isInsideDir` (uses `path.sep` boundary so `.../LifeOS` doesn't match `.../LifeOS-backup/`). Verified against 15-case smoke test covering decision + confirm-count parity. `deny` zone policy was considered and cut — `blockPath`'s equality semantics don't compose with directory-block intent; add a `blockInsideDirs` rule kind when a real agent needs read-only access inside its workspace. `AgentZone` re-exported from `backend/agent/index.ts`. Docs: new D12 in `AGENT-DESIGN.md` (documents the custom-first merge order + trimmed baseline + dropped `deny`); D7 amended from "vault ≠ config" to "vault ≠ runtime state" (per-agent vault configuration like skills lands under `$VAULT/InkStone/`, runtime state stays under `~/.config/inkstone/`); `ARCHITECTURE.md` Permission Dispatcher + base-layer + agents-on-ship + adding-a-new-agent sections updated. Skills loader, `references?` field, config-path centralization, and `blockInsideDirs` remain in Future Work.

- [x] Merge master into `refactor/agent-shell-base-layer`. Integrates the SQLite persistence layer (from master) with the agent-shell refactor series (base layer, folder-per-agent, `AgentCommand`, unified command registry, pi-coding-agent tools, declarative permission dispatcher). Reconciled three conflicts: (a) `src/tui/context/agent.tsx` — adopted master's SQLite framework (ensureSession, runInTransaction writers, raw AgentMessage restore, legacy-JSON importer) then overlaid the branch's `BridgeAgentCommands` component; removed `AgentActions.loadArticle` (now reader's `articleCommand` per D9); session restore calls `setActiveArticle(loaded.session.activeArticle)` directly instead of the defunct `actions.loadArticle(...)`. (b) `AgentCommandContext` extended with `setActiveArticle(id)` — centralizes the three side effects of an activeArticle change (backend module state, solid store mirror, SQLite session-row update) so reader's `articleCommand` doesn't touch any of them directly. Acknowledged reader-vocabulary leak; when a second agent needs similar wiring this becomes the point where a generic `ctx.syncStore(key, value)` replacement lands. (c) Reader's `articleCommand` now calls `ctx.setActiveArticle(id)` instead of the module-level import, removing the microtask-mirror hack that was the alternative. Docs merged: unioned completed items across both branches; Future Work combines both sets (master's SQLite items + branch's slash / memory / skills / extra-args-guard items); dropped master's older "Robust slash-command parsing" entry because the branch's version is strictly newer (acknowledges `triggerSlash` + registry unification shipped).

- [x] Declarative permission dispatcher + agent overlay. New `src/backend/agent/permissions.ts` owns the policy layer: a tagged-union `Rule` type (`insideDirs` / `confirmDirs` / `blockPath` / `frontmatterOnlyFor`), `registerBaseline(toolName, rules)` for tool-level defaults, `dispatchBeforeToolCall(ctx, overlay?)` as the single pi-agent-core beforeToolCall hook, plus the `resolvePath` / `getFrontmatter` helpers lifted from the former guard. `tools.ts` registers baselines at module load (`read` → `insideDirs [VAULT_DIR]`; `write`/`edit` → `insideDirs + confirmDirs [NOTES_DIR, SCRAPS_DIR]`). `AgentInfo` grows an optional `getPermissions?(): AgentOverlay`; reader supplies one that returns `blockPath` + `frontmatterOnlyFor` keyed on `activeArticle` (empty object when no article is open). `backend/agent/index.ts`'s `beforeToolCall` wiring collapses from the `_articlePath` args-injection hack + a 135-line `guard.ts` to a one-liner that calls the dispatcher with the active agent's overlay. `guard.ts` deleted. `_articlePath` grepps to zero hits. Rules are pure data; only the overlay *factory* is a function (accepted trade-off — D11 explains). Behavior parity verified against the pre-dispatcher guard via a 20-assertion smoke test covering baseline sandbox (read/write/edit outside VAULT_DIR blocked), `~` + `@` path expansion (would-bypass cases blocked), reader overlay (write on active article blocked; edit on frontmatter allowed; edit on body blocked; multi-edit with mixed targets blocked; non-article edit passes), and example agent having no overlay. Docs: `ARCHITECTURE.md` Guard Logic section replaced with a Permission Dispatcher section; new D11 in `AGENT-DESIGN.md`; file-structure diagram updated.

- [x] Remove `refreshSystemPrompt` from the public API. It was a manual cache-invalidation call papering over the fact that module-scoped agent state (reader's `activeArticle`) and the cached `Agent.state.systemPrompt` string could drift silently. Replaced with a pull model: `AgentActions.prompt(text)` now composes `systemPrompt = composeSystemPrompt(getAgentInfo(currentAgent))` at the start of every turn, so any state mutated before the call is picked up automatically. Reader's `/article` command drops `ctx.refreshSystemPrompt()`; session restore in `tui/context/agent.tsx` drops the equivalent direct call; `AgentCommandContext` narrows from `{ prompt, refreshSystemPrompt }` to `{ prompt }`. `setAgent()` + `clearSession()` keep their inline rebuilds for correctness on mid-session reads of `a.state.systemPrompt` (the next `prompt()` wrapper would rebuild anyway — byte-identical). `AgentActions` loses one method and the TUI wrapper's trampoline. Docs: `ARCHITECTURE.md` Commands section + Base-layer paragraph, `AGENT-DESIGN.md` D9 shape block + narrative, `SLASH-COMMANDS.md` Path A shipped note.

- [x] Adopt `@mariozechner/pi-coding-agent` tools. Inkstone no longer maintains its own `readFileTool` / `editFileTool` / `writeFileTool` / `quoteArticleTool`. New `src/backend/agent/tools.ts` is a thin pool that calls `createReadTool(VAULT_DIR)` / `createWriteTool(VAULT_DIR)` / `createEditTool(VAULT_DIR)` from `@mariozechner/pi-coding-agent` and re-exports them as `readTool` / `writeTool` / `editTool`. `BASE_TOOLS = [readTool]`; reader's `extraTools = [editTool, writeTool]`. `quote_article` is gone — the LLM uses `read` + its own context window to quote; reader's instructions updated accordingly. Base layer flattened: `src/backend/agent/base/index.ts` → `src/backend/agent/base.ts` with the `base/tools/` folder deleted. Agent folder `agents/reader/tools/` deleted entirely. Guard (`backend/agent/guard.ts`) updated: (1) tool-name pattern match switched to `read` / `edit` / `write`; (2) article-edit rule iterates pi-coding-agent's multi-edit schema (`args.edits: [{ oldText, newText }, ...]`), requiring every `oldText` to hit the frontmatter; (3) path resolution inlines pi-coding-agent's `~` / `@` expansion so `startsWith(VAULT_DIR)` cannot disagree with the tool's own resolution (would otherwise be a sandbox bypass — `~/foo` literal vs `$HOME/foo` expanded). Dependency bumps: `@mariozechner/pi-agent-core` `^0.67.68 → ^0.69.0`, `@mariozechner/pi-ai` added explicitly at `^0.69.0`, `@mariozechner/pi-coding-agent` added at `^0.69.0`. pi-tui arrives as a transitive dep of pi-coding-agent; its render hooks are stripped by `wrapToolDefinition` so it's inert at runtime but loaded at module-resolve time — acceptable cost for dropping four hand-maintained tool files. Provider prompt caches bust once on first turn after deploy because tool names changed and count went from 4 → 3 on reader; unavoidable. New D10 in `AGENT-DESIGN.md` captures the rationale; `ARCHITECTURE.md` file structure + Base-layer + Guard Logic + Agents-on-ship updated; `PLAN.md` File Tool Specs updated.

- [x] Unify command registry — SLASH-COMMANDS.md Path A, registry half. Resolves the asymmetry where `clearSession` existed on `CommandContext` only to service a trampoline (`BUILTIN_COMMANDS`'s `clear.execute` did nothing except call `ctx.clearSession`). The backend `AgentCommand` + TUI `CommandOption` surfaces are now one registry. `CommandOption.slash?: SlashSpec { name, takesArgs?, argHint? }` + `command.canRunSlash` / `command.triggerSlash` on the provider API. `/clear` is a `CommandOption` registered by `Layout()` in `src/tui/app.tsx` closing over `actions.clearSession`. Backend `AgentCommand.execute` now takes a narrow `AgentCommandContext { prompt, refreshSystemPrompt }` — no more `clearSession` or `abort`. `BridgeAgentCommands` (child component inside `AgentProvider`) reactively bridges the current agent's `AgentInfo.commands` into `CommandOption[]`; argful commands register `hidden: true` so they don't show in Ctrl+P. Dispatch precedence preserved via registration order — agent-bridge registers first, so agent-scoped slashes beat shell-scoped on name collision. Deleted: `BUILTIN_COMMANDS`, `AgentActions.runAgentCommand`, `AgentActions.canRunAgentCommand`, `findAgentCommand`, `hasRequiredCommandArgs`, the dual manual+fallback `CommandContext` construction in backend/TUI. Docs: new Commands section in `ARCHITECTURE.md`; rewritten D9 in `AGENT-DESIGN.md`; `SLASH-COMMANDS.md` updated with Status and §6 Path A shipped. Verified with `bun run ci`, `tsc --noEmit`, module-load smoke test, and slash-dispatch logic unit-test. TTY-interactive smoke-test not possible in this environment — first real launch will exercise the new path.

- [x] Agent import-boundary lint rules. `biome.json` now blocks `backend/agent/index.ts` and `backend/agent/agents.ts` from deep-importing custom-agent tool internals (`agents/<name>/tools/*`); agent-owned public state/helpers must be re-exported from the agent folder `index.ts`. TUI files now fail lint if they import custom-agent internals under `@backend/agent/agents/**`, keeping the frontend on `@backend/agent` public APIs. `ARCHITECTURE.md` and `PLAN.md` document the enforced constraint. Verified with `bun run ci`, `tsc --noEmit`, and temporary negative imports that produced the intended Biome diagnostics before removal.

- [x] Slash-command review fixes. TUI-run agent commands now pass a `CommandContext` backed by `wrappedActions`, so `/clear` goes through the same UI/session-clearing path as the direct action and `/article <file>` starts its generated prompt through the wrapper that adds the user bubble, stamps `lastTurnStartedAt`, and catches pre-stream errors. `runAgentCommand` still has a backend fallback context for non-TUI callers, but also exposes `canRunAgentCommand(name, args?)` so the prompt submit path only intercepts executable slash commands. Unknown slash text (`/help`, `/article foo` on Example) and missing required args (`/article`) now fall through as normal prompts instead of getting trapped or dropped. Docs updated in `ARCHITECTURE.md` and `AGENT-DESIGN.md`; verified with `bun run ci` and `tsc --noEmit`.

- [x] `AgentCommand` — agents declare user-facing verbs distinct from tools. Resolves the "Reader-specific vocabulary leaks onto `AgentActions`" pressure point along all three axes: (verb) `AgentActions.loadArticle` removed, reader declares `articleCommand: AgentCommand`, shell dispatches via `AgentActions.runAgentCommand(name, args?)`; (state) `activeArticle` module state moved from `backend/agent/index.ts` into `agents/reader/index.ts`; (context) `AgentBuildContext` dropped — `buildInstructions()` is nullary and reads owned state directly. New types in `base/`: `AgentCommand { name, description?, argHint?, takesArgs?, execute }`, `CommandContext { prompt, abort, clearSession, refreshSystemPrompt }`, `AgentInfo.commands?`, `BUILTIN_COMMANDS = [clearCommand]` (frozen). Agent-scoped commands take precedence over built-ins on name collision. TUI's `prompt.tsx` submit handler replaces the hardcoded `/clear` and `/article ` branches with `actions.runAgentCommand(name, args)` dispatch; unknown-command errors restore the typed text so the user can edit or resubmit as plain prompt. `AgentActions.refreshSystemPrompt()` added for callers outside the dispatcher that mutate state directly (session restore). Docs: new decision D9 in `AGENT-DESIGN.md`; new "Commands" subsection in `ARCHITECTURE.md`; Terminology gains `Command` and `Tool` as first-class entries. Dialog-opening commands (`/models`, `/themes`, `/connect`, `/agents`, `/effort`) stay as `CommandOption` entries in the TUI palette — they need `DialogContext` which can't cross the layer boundary; future slash-dropdown work (tracked in `docs/SLASH-COMMANDS.md`) may unify the user-facing surface.

- [x] Extract memory + skills design exploration to dedicated docs. `docs/MEMORY.md` captures the two-file memory system (`user.md` + `memory.md` under `~/.config/inkstone/`, read path + write path as separate design surfaces); `docs/SKILLS.md` captures the SKILL.md lazy-load system (front-matter + body, `~/.config/inkstone/skills/`, agent-created skills intent, filtering model open questions). `AGENT-DESIGN.md`'s "Anticipated pressure points" section shrinks — Skills and Memory subsections become 1-line pointers to the new docs. Mirrors the `SLASH-COMMANDS.md` extraction pattern.

- [x] Agent design doc polish + `BASE_TOOLS` hardening. (Doc) `AGENT-DESIGN.md`'s "Extension points" section rewritten as "Anticipated pressure points" — each deferred feature (skills, memory read/write, reader-vocabulary leak, web search, per-agent permissions) now names the pressure it's resolving and the open questions at implementation time, without pre-committing to field names, function signatures, or tool names. Added a new decision **D8 — Deferred features are pressure points, not APIs**, extending D5's "ship mechanism, defer content" principle to the future-work documentation surface. Memory files section split into read (low-risk, universal candidate) vs write (sensitive, needs explicit/auto, confirmation, overwrite-semantics, review-flow, per-agent-gating decisions before an API lands). Terminology glossary dropped the "Session actions" entry that pre-committed to `runAgentAction`; added "Pressure point" as a first-class term. (Code) `BASE_TOOLS` is now exported as `readonly AgentTool<any>[]` and wrapped in `Object.freeze(...)` — the "`base/` owns what's in `BASE_TOOLS`" invariant is enforced at the language level (mutation attempts throw in strict mode; `composeTools` continues to return a fresh array via spread, so compositions are unaffected). TODO's old `registerBaseTool` Future Work entry resolves into this completion entry. The session-actions Future Work entry is reframed to describe the pressure (reader-shaped vocabulary leaking onto a generic action surface) instead of naming an API.

- [x] `docs/AGENT-DESIGN.md` — dedicated design-rationale doc for the agent system. Captures the seven key decisions (composition over inheritance, folder-per-agent layout, base-layer bundle, no opt-out on `BASE_TOOLS`, ship-mechanism-defer-content, `AgentInfo` as data not a class, vault ≠ config), rejected alternatives with reasoning, extension points where future features (skills, memory, per-agent session actions, `BASE_TOOLS` immutability, web search, per-agent permissions) are designed to plug in, and a terminology glossary disambiguating "base agent" (a layer, not a class), "custom agent", `extraTools`, "foundation tools", "skills", "memory files", "session actions". `ARCHITECTURE.md`'s "Agent Registry" section gains a pointer to the new doc so readers land on the "why" when they want it.

- [x] Agent shell — review follow-ups. (P2) `setActiveArticle` is now re-exported from `backend/agent/agents/reader/index.ts`; the shell in `backend/agent/index.ts` imports it from `./agents/reader` instead of `./agents/reader/tools/quote-article`, so the "generic" agent module no longer reaches into a specific agent's internal tool file. `grep` in `backend/agent/index.ts` for `agents/reader/tools` returns no matches. This is a scaffolding fix — the deeper issue (`AgentActions.loadArticle` being reader-shaped vocabulary on a supposedly generic interface) is tracked in Future Work. (P3) Reader's `extraTools` reordered to `[editFileTool, writeFileTool, quoteArticleTool]` so that with `BASE_TOOLS = [readFileTool]` prepended by `composeTools`, the composed tool order is `[read_file, edit_file, write_file, quote_article]` — byte-identical to pre-refactor. Matters for provider prompt-cache stability (Anthropic / OpenAI / Bedrock cache by byte-exact tools prefix, so any reorder forces a one-time cache rebuild on affected providers).

- [x] Agent shell refactor — introduce a base layer + folder-per-agent structure. Flat registry + runtime composition, no inheritance. `backend/agent/base/` now owns the foundation tools (`read_file`), the empty-today `BASE_PREAMBLE`, the shared `AgentInfo` type, and the `composeTools` / `composeSystemPrompt` helpers. Each custom agent lives in its own folder (`backend/agent/agents/reader/`, `backend/agent/agents/example/`) — self-contained with `index.ts`, optional `instructions.ts`, and optional `tools/` subdirectory. `backend/agent/agents.ts` is reduced to a thin registry assembler. The four composition call sites (`getAgent`, `setAgent`, `loadArticle`, `clearSession`) in `backend/agent/index.ts` now route through the composers. Adding a new agent is now one folder + one line in `agents.ts` — no changes to `base/`, `index.ts`, the TUI, or config schemas. Only user-visible behavior change: the `example` agent gains `read_file` via base (its 1-line prompt drops the now-stale "You have no tools available" sentence). Reader's emitted system prompt is byte-identical to pre-refactor (confirmed with a string-compare test at both `activeArticle: null` and `activeArticle: "test.md"`). Composed tool-declaration order is also preserved as `[read_file, edit_file, write_file, quote_article]` — see the follow-up entry above. Shell-only scope — skills system, memory system, `memory_write`, `web_search`, and per-agent permission rulesets are deliberately deferred for future PRs; `base/` is the single extension point where they'll land.

- [x] Second persistence review pass. Follow-up fixes after the first review surfaced deeper design issues: (1) **`message_end` commits atomically** via new `runInTransaction(fn)` — all writers accept an optional `tx` handle, and the reducer now wraps the header-meta update + parts finalize + raw-AgentMessage append in one transaction. A mid-trio crash rolls back instead of leaving an orphan shell, eliminating the repair path for new corruption. (2) Extracted **`repairSession(id)` from `loadSession`** — `loadSession` is now a pure read. `AgentProvider` calls `repairSession` explicitly on boot before hydration. Keeps the empty-shell pruning for pre-transaction data and any path that sidesteps the reducer. (3) **Dropped `events` table entirely.** Writers existed (model/thinking-level changes) with zero readers — same speculative-scaffolding pattern we called out for memory. Gone from schema, sessions.ts, and the reducer. Will re-add with a real history/timeline reader. (4) **Dropped `messages.parent_id` column.** It was a linked list pretending to be a tree: every new message's "parent" was just the previous row, which `ORDER BY id` already encodes. `parent_id = previous-message` would actively mislead once branching lands. No schema tree until branching arrives with real requirements. (5) **Added `agent_messages.display_message_id`** — nullable FK back to `messages.id`, populated when an assistant AgentMessage produces a bubble (NULL for tool-result/user/custom). Cheap to add now; painful to backfill later. (6) **Fixed `shortId`** — uses the last 8 hex (random tail) instead of the first 8 (ms timestamp prefix). Every id written in the same minute used to alias; now each error toast identifies a unique message. (7) `0000_init.sql` regenerated from the new schema (Option 2: no users to migrate). (8) `docs/SQL.md` rewritten — trimmed column tables to one-liners that link to `schema.ts`, promoted the pre-1.0 reset callout to a top-level `> DANGER` block, deleted the speculative "Future Schema Directions" section (contents already in TODO), collapsed Migration Bundling to one line, added `$type<>` TS-only disclaimer, fixed the UUIDv7 sub-ms caveat, restated the `currentSessionId` invariant flatly. (9) Added "Known limitations" section calling out the concurrency gap (double-launch interleaving writes) — documented, not fixed.

- [x] Persistence review-pass fixes (first round). Bugs + dead-code cleanup from the initial post-PR review of the SQLite layer: (1) dropped `uuidv7` npm dep — `Bun.randomUUIDv7()` is stdlib since 1.1.25. (2) Deleted `sessions.cwd` column — we went global, cwd was noise. (3) Deleted `backend/persistence/memory.ts` — 65 lines of API scaffold with zero callers. (4) Regenerated `0000_init.sql` (Option 2: no users to migrate pre-1.0). (5) `loadSession` parts fetch now uses `inArray(parts.messageId, ids)`. (6) `listSessions` counts via a single `GROUP BY session_id` query. (7) Load-time repair strips trailing empty assistant shells. (8) Split the old `updateDisplayMessage` into `updateDisplayMessageMeta` + `finalizeDisplayMessageParts`, killing the DELETE+INSERT thrash on every `message_end`. (9) `message_start` writes shell-only via `{ includeParts: false }`. (10) Richer persistence-error toasts — message-id prefix + event type embedded. (11) `errors.ts` + `schema.ts` docstrings updated. (12) Deleted dead `hydrateStoreSeed`, empty `if (loaded) {}`, redundant `newMessageId` wrapper.

- [x] SQLite persistence layer (Phase 6). Replaced single-file `~/.local/state/inkstone/session.json` with `~/.local/state/inkstone/inkstone.db` backed by `bun:sqlite` + `drizzle-orm`. See `docs/SQL.md` for the design.

- [x] Config refactor — move `backend/persistence/` → `backend/config/`, add Zod v4 as a direct dep, and introduce `config/schema.ts` with `strictObject` schemas for `config.json` and `auth.json`. Parse failures (wrong field type, unknown top-level key, bad `thinkingLevels` enum value, malformed JSON) now surface a field-level error through `reportPersistenceError` and fall back to defaults so the app still boots. `errors.ts` `kind` widens to `"config" | "auth" | "session"` and covers `action: "load"` in addition to save/clear; load failures hit the built-in `console.error` path since they fire before the frontend wires its toast handler. New `config/paths.ts` deduplicates the XDG logic previously repeated in `config.ts` / `auth.ts`. `SessionData` deliberately stays un-schema'd — it's an internal Inkstone type with no untrusted-input boundary. Six call sites updated with pure import-path renames; no function-name changes.

- [x] REVIEW.md follow-ups: (1) `VAULT_DIR` is now platform-neutral — reads `config.vaultDir` from `~/.config/inkstone/config.json` when set, otherwise falls back to `~/Documents/Obsidian/LifeOS` via `os.homedir()`. Display of the vault path in `open-page.tsx` and `sidebar.tsx` routed through a new `displayPath()` helper in `tui/util/format.ts` that collapses the real home dir to `~` on both Linux (`/home/<user>`) and macOS (`/Users/<user>`). (2) `AgentProvider.wrappedActions.prompt` now wraps `actions.prompt(text)` in try/catch; pre-stream throws (e.g. `getApiKey()` rejection before `message_start`) reset `isStreaming`/`status`, stash the error on the last assistant bubble (or push a synthetic one if none exists), and surface a toast. (3) New `backend/persistence/errors.ts` module with `setPersistenceErrorHandler` + `reportPersistenceError`; `saveConfig`, `saveSession`, and `clearSession` now wrap their `writeFileSync` calls in try/catch and route failures through the hook. `AgentProvider` wires the hook to `useToast()` so disk-full / permission-denied errors surface as error toasts instead of crashing. (4) `dialog.replace` input parameter and the store's `element` field are now typed as `() => JSX.Element` instead of `any`. (5) Deleted dead code `tui/context/helper.tsx` (never imported). (6) `dialog-confirm.tsx` now handles `y` / `n` keyboard shortcuts in addition to `left` / `right` / `return`, matching the documented keybind surface.
- [x] Handle pi-kiro's `Reasoning hidden by provider` redacted-thinking placeholder. Extended the `thinking_end` pop predicate in `context/agent.tsx` to strip all known placeholder strings via a `REDACTED_THINKING_PLACEHOLDERS` array (previously only `[REDACTED]` was handled). Updated JSDoc in `bridge/view-model.ts` to document both placeholder shapes and their origins.

- [x] Thinking-block review fixes: (a) `thinking_end` pop predicate in `context/agent.tsx` now strips the `[REDACTED]` literal before trimming, so OpenRouter's `[REDACTED]`-as-delta shape collapses to empty and gets popped (previously `"[REDACTED]".trim()` was truthy, so the block would have rendered verbatim). Anthropic's zero-delta `redacted: true` path still covered. (b) Reverted leftover `KIRO_LOG=debug KIRO_LOG_FILE=/tmp/inkstone-kiro.log` from the `dev` script in `package.json`. (c) Documented reducer-guaranteed `part.type` immutability in `docs/ARCHITECTURE.md` so future contributors know the non-reactive `<For>` branch in `ReasoningPart` / `TextPart` dispatch is intentional.
- [x] Thinking-block `subtleSyntax` port. Added `thinkingOpacity: number` (0.6) to `ThemeColors` on all four themes and `generateSubtleSyntax(colors)` in `src/tui/context/theme.tsx` — rebuilds every `getSyntaxRules` rule's foreground at `thinkingOpacity` alpha, preserving per-scope hue. `useTheme()` now exposes `subtleSyntax: Accessor<SyntaxStyle>` alongside `syntax`, with the same `onCleanup(() => style.destroy())` FFI lifecycle. `conversation.tsx` `ReasoningPart` rewritten to match OpenCode's `routes/session/index.tsx:1437-1468` byte-for-byte (minus `<code filetype="markdown">` → `<markdown>` for renderer consistency): single `<markdown content="_Thinking:_ …" syntaxStyle={subtleSyntax()}>`, border `theme.backgroundElement`, no outer `fg` override. Dropped orphaned `TextAttributes` import.
- [x] Thinking-block review pass — Pass 1 correctness + Pass 2 polish. Typed `assistantMessageEvent` as pi-ai's `AssistantMessageEvent` union (inner `switch` picks up exhaustiveness via TS, with an explicit `default: break;` for the variants Inkstone intentionally ignores: `start`, `text_end`, `toolcall_*`, `done`, `error`). Added tail-part type guards in `text_delta` / `thinking_delta` arms so a future upstream event reordering can't silently cross-append into the wrong block. Dropped the `[REDACTED]` regex in `thinking_end`: Anthropic's `redacted: true` path fires no `thinking_delta` at all, and OpenRouter's `[REDACTED]` literal collapses under `.trim()`, so popping on `!lastPart.text.trim()` handles both paths without a regex. `migrateMessage` now destructures the legacy `text` field out of the spread so it doesn't round-trip through `saveSession` on the next turn. Pass 3 (margin stacking): confirmed OpenCode puts `marginTop={1}` on every part unconditionally (`routes/session/index.tsx:1450,1475`); Inkstone's `marginTop={0}` on the first part is an intentional divergence because assistant parts render inside a `<box flexDirection="column">` under an outer `<For>` with `gap={1}`, and an unconditional first-part `marginTop` would double-space against the outer gap. Documented in `docs/ARCHITECTURE.md`.
- [x] Thinking (reasoning) block rendering. `DisplayMessage.text: string` replaced with `parts: DisplayPart[]` (discriminated `text` | `thinking`) so interleaved blocks from a single turn render in emission order. `context/agent.tsx` reducer branches `message_update` on `assistantMessageEvent.type`: `text_start` / `thinking_start` push a fresh part, `*_delta` append to the tail part, `thinking_end` pops the part when its accumulated `text.trim()` is empty (covers OpenRouter's encrypted path + Anthropic's `redacted: true`). Renderer in `conversation.tsx` dispatches per-part: thinking renders with a `borderSubtle` left bar, `paddingLeft={2}`, `fg={theme.textMuted}`, italic `_Thinking:_ ` prefix — ported from OpenCode's `ReasoningPart` (`routes/session/index.tsx:1437-1468`). `streaming` flag flows only to the tail block of the last bubble. No toggle, no keybind — always rendered when present. Legacy persisted sessions with `text: string` migrate on load to a single text part. Trimmed from OpenCode: `subtleSyntax()` (60%-alpha variant), `thinkingOpacity` theme knob, transcript/export parity.
- [x] Amazon Kiro provider (`backend/providers/kiro.ts`). Wraps `pi-kiro/core` — registers `kiro-api` with pi-ai's api-registry at module load, returns region-scoped models via `filterModelsByRegion` + `baseUrl` rewrite, and does lazy token refresh in `getApiKey()` (no background scheduler). Credentials persist to `~/.config/inkstone/auth.json` (mode 0600, dir 0700) via a new `backend/persistence/auth.ts` module keyed by provider id so future OAuth providers slot in. `ProviderInfo.getApiKey` widened to `string | undefined | Promise<string | undefined>`; Bedrock's sync implementation still compiles (pi-agent-core already awaits). Boot fallback widened: if a stored OAuth provider's default model no longer resolves (cleared creds), the agent module falls back to `DEFAULT_PROVIDER` instead of throwing — the user can re-connect from Connect. See `docs/ARCHITECTURE.md` "Provider Registry → Kiro provider" for the flow.
- [x] Connect command palette activation for Kiro. New `DialogPrompt` + `DialogAuthWait` UI primitives (`src/tui/ui/`) drive pi-kiro's `loginKiro` callbacks against the existing dialog stack via `dialog.replace`, so only one dialog is on the stack at any time (sidesteps pi-kiro's documented mirrored-cursor glitch from `oauth.ts:16-23`). ESC at any step aborts via an `AbortController` passed to `loginKiro`; pi-kiro throws "Login cancelled" which is swallowed silently. On success the flow chains into `DialogModel` scoped to Kiro so the user lands on the freshly-available catalog (mirrors OpenCode's `dialog-provider.tsx:183-184`).
- [x] Phase 0: Project scaffolded, deps installed, docs created
- [x] Phase 1: App shell (theme, dialog, toast, components)
- [x] Phase 2: Agent integration (Bedrock streaming verified)
- [x] Phase 3: File tools + guard + /article command
- [x] Phase 4: Model selection dialog (Ctrl+M, fuzzy search)
- [x] Phase 5: Session persistence (JSON save/restore, /clear command)
- [x] Fix: streaming text in-place growth (produce pattern from OpenCode)
- [x] Fix: focus management (prompt always focused, click-refocus, scroll keybinds)
- [x] Last-turn status line between conversation and prompt (agent, model, duration, input/output tokens)
- [x] Markdown rendering for assistant messages (concealed syntax markers, inline/block code styling, list/quote/heading formatting, reactive to theme switch)
- [x] Fix: destroy stale `SyntaxStyle` on theme switch and provider teardown to release FFI-backed native resources
- [x] Fix: per-message status line (agent/model/duration stored on each `DisplayMessage`, rendered below every assistant reply, survives model switches and session restores)
- [x] Fix: footer boundaries follow pi-agent-core's `message_start`/`message_end` events — one display bubble per assistant message boundary, with model name sourced from the event itself (not the mutable `store.modelName`). Tool-driven turns now show a distinct, correctly-labeled footer for each assistant reply, and mid-stream Ctrl+P model switches do not relabel in-flight replies.
- [x] Docs: clarify `DisplayMessage` footer field scopes — `agentName`/`modelName` are per-message (stamped in `message_end` from the assistant event), `duration` is per-turn (stamped in `agent_end` on the turn-closing assistant bubble only). Intermediate assistant bubbles in tool turns correctly persist/render without a duration pip.
- [x] Refactor: split `src/` into three layers with enforced dependency direction — `backend/` (headless agent, tools, persistence), `bridge/` (shared view-state types: `DisplayMessage`, `AgentStoreState`, `SessionData`), `tui/` (Solid + OpenTUI UI). Dependency graph: `tui → bridge, backend`; `backend → bridge` (types only); `bridge → nothing`. Enables parallel backend/frontend work and sets up a swappable frontend in the future.
- [x] Tooling: add Biome (lint + format) with path aliases (`@backend/*`, `@bridge/*`, `@tui/*`) and `noRestrictedImports` overrides that mechanically enforce the layer boundary rules. `bun run ci` is the single CI-ready command. Mass format pass migrated the codebase to Biome defaults (tabs, semicolons, 80-char lines).
- [x] Multi-agent shell: static registry (`backend/agent/agents.ts`) with two agents — `reader` (existing Obsidian reading guide, 4 tools, `secondary` accent) and `example` (placeholder chat assistant, no tools, `accent` accent). `Tab` / `Shift+Tab` cycle agents on the open page; switching is locked once the session has messages (diverges from OpenCode's always-on `agent_cycle` to match Inkstone's "one agent per session" model). Current agent persists to `config.json` alongside `modelId`/`themeId`. Prompt label + input border + user-bubble border + assistant-footer `▣` glyph all derive their color from the active agent's `colorKey`. Command-palette entry shown only on the open page. Bubble-footer `agentName` now stamped with the active agent's `displayName` at `message_end`.
- [x] Fix (review): `/article` is now gated on the reader agent — on any other agent the text falls through as a normal prompt (avoids a broken reading flow under an agent that has no article tools).
- [x] Fix (review): `currentAgent` is persisted inside `session.json` and wins over `config.json` on restore, so a transcript always reopens under the agent that produced it, regardless of any intervening config drift. Legacy sessions without the field still fall through to config.
- [x] Keybind registry + command provider: central `src/tui/util/keybind.ts` with a `KEYBINDS` action map and pure `match(action, evt)` / `print(action)` helpers; `CommandProvider` in `src/tui/components/dialog-command.tsx` with a reactive `register(() => CommandOption[])` API. Ctrl+P palette is now fully registry-driven (registered by `Layout()` in `app.tsx`). Fixes a latent bug where Ctrl+P inside an open DialogSelect could re-stack the palette, via CommandProvider's `dialog.stack.length === 0` guard. DialogSelect nav supports emacs-style Ctrl+P/Ctrl+N on top of arrow keys. Prompt hints render via `Keybind.print(...)` so labels stay in sync with bindings. Still deferred: user overrides, leader-chord, plugin keybinds, textarea action mapping.
- [x] Provider registry: `src/backend/providers/` with a `ProviderInfo` interface and Amazon Bedrock as the first concrete provider. Wraps pi-ai's `getModels` / `getEnvApiKey` for Bedrock; designed to accommodate custom providers (e.g. Bedrock-Converse-compatible endpoints like Amazon Kiro) by having each provider return hand-built `Model<Api>[]` with a custom `baseUrl`. `backend/agent/index.ts` no longer hardcodes `"amazon-bedrock"` — active state is `(providerId, modelId)`, `getApiKey` dispatches through the registry, `setModel(model)` persists both ids, and `resolveModel()` looks up live `Model<Api>` objects through the registry. Legacy `config.json` with only `modelId` falls back to the default provider.
- [x] Two-dialog provider flow: **Connect** (`DialogProvider`) lists every registered provider sorted connected-first, with `"✓ Connected"` / `"Not configured"` descriptions; selecting a disconnected provider toasts the provider's `authInstructions`; selecting a connected provider closes the dialog (no-op — reserved for future disconnect/manage). **Models** (`DialogModel`) lists only models from *connected* providers (flat list, categories populated for future grouping), with an empty-state hint when nothing is connected. `AgentStoreState.modelProvider` now holds the raw provider id; the prompt bar formats via `getProvider(id).displayName` at render time.
- [x] Fix (review): curated per-provider default model. `ProviderInfo` gained a required `defaultModelId` field — the boot fallback no longer picks `listModels()[0]` (which was `amazon.nova-2-lite-v1:0` under pi-ai's current ordering) and instead uses the provider's declared default. Bedrock defaults to `us.anthropic.claude-opus-4-7`. If the declared default no longer resolves through pi-ai, the agent module throws on boot so registry drift surfaces loudly instead of silently.
- [x] Fix (review): broader Bedrock connection probe. `isConnected()` now also returns true when `~/.aws/credentials` or `~/.aws/config` exists (honoring AWS_SHARED_CREDENTIALS_FILE / AWS_CONFIG_FILE overrides), not just when an AWS env marker is set. Users with a `[default]` profile from `aws configure` / `aws sso login` no longer see Bedrock falsely marked "Not configured" and their models are no longer hidden from the picker.
- [x] Per-model reasoning-effort variants (OpenCode-style dedicated entry). Reasoning-capable models expose a dedicated **Effort** palette entry (between Models and Themes) that opens `DialogVariant` on the current model and lets the user pick a pi-agent-core `ThinkingLevel` — `"off" | "minimal" | "low" | "medium" | "high"`, plus `"xhigh"` for `supportsXhigh(model)`-true models (Claude Opus 4.6/4.7, GPT-5.2+). Non-reasoning models hide the entry from the palette via `store.modelReasoning` (mirroring OpenCode's `hidden` flag on `variant.list` in `opencode/src/cli/cmd/tui/app.tsx:537`). Selected level is persisted per-model in `config.thinkingLevels: Record<"${providerId}/${modelId}", ThinkingLevel>` and re-applied automatically on model switch via `setModel`'s auto-restore. The active effort renders as a bold warning-toned suffix (`· high`) in the prompt status line when non-off, matching OpenCode's statusline variant badge (`prompt/index.tsx:1204-1211`). Under the hood, pi-agent-core's `Agent.state.thinkingLevel` plumbs the level to pi-ai's unified `reasoning:` stream param (pi-ai owns the per-provider mapping to `reasoning_effort` / `thinking.budgetTokens` / `thinkingConfig.thinkingLevel` / etc., including silent collapses like `minimal → "low"` on adaptive Claude that produce identical model behavior). Deferred (not built): mid-session cycle keybind, per-message footer stamping, user-configurable per-model level lists.
- [x] Docs: add a dedicated E2E testing plan file covering the chosen test layer and first feature targets (`docs/E2E-PLAN.md`)
- [x] Fix: surface assistant-turn errors in the conversation bubble. pi-ai converts provider SDK exceptions (e.g. Bedrock `ValidationException` when a model id requires an inference profile and isn't invocable on-demand) into stream `error` events, which pi-agent-core forwards through `message_end` with `AssistantMessage.stopReason === "error"` and `errorMessage` populated. Inkstone's `message_end` handler now stashes `errorMessage` onto `DisplayMessage.error`, and `conversation.tsx` renders a warning-bordered panel (left border in `theme.error`, muted body text) below the assistant body whenever `msg.error` is set. The outer render gate widens from `msg.text` to `msg.text || msg.error` so errored bubbles with empty content still render. Mirrors OpenCode's per-message error surface (`routes/session/index.tsx:1374-1387`). Covers both `stopReason === "error"` and `"aborted"`; differentiating aborts via a muted `· interrupted` footer suffix is deferred.
- [x] ESC double-tap to interrupt streaming turn. New `session_interrupt: "escape"` keybind registered by `prompt.tsx` via `useCommand().register`, gated on `store.isStreaming` so the binding is live only during a turn. First press flips the prompt hint from `esc interrupt` to `esc again to interrupt` in `theme.primary` and arms a 5s timer to reset; second press within the window calls `actions.abort()` (pi-agent-core `Agent.abort()`), which stops the stream and fires a `message_end` with `stopReason === "aborted"` — already surfaced via the existing error-panel path (`context/agent.tsx:153-165` + `components/conversation.tsx:81-99`). Counter state is local to `Prompt()` (no new field in `AgentStoreState`); timer is cleared on component unmount. A `createEffect` also resets counter + timer whenever `store.isStreaming` transitions to false, scoping the arm to a single turn — a single ESC late in a response that completes before the timer fires would otherwise leave `interrupt === 1`, causing the first ESC of the next turn to abort immediately instead of arming the double-tap (intentional divergence from OpenCode, which has this latent bug). Dialog ESC (`dialog_close`) still wins when a dialog is on the stack via `CommandProvider`'s `dialog.stack.length > 0` short-circuit. Matches OpenCode's `prompt/index.tsx:273-303, 1325-1330`.
- [x] OpenCode-style knight-rider spinner in the prompt hints row. Replaced the single braille dot next to `esc interrupt` with an 8-cell bidirectional wave of colored blocks colored by the current agent (`theme[agentInfo().colorKey]`). Port of OpenCode's `ui/spinner.ts` blocks / bidirectional branches (`../opencode/packages/opencode/src/cli/cmd/tui/ui/spinner.ts:141-329`), wired the way `component/prompt/index.tsx:927-946, 1262-1265` drives the upstream `<spinner>` renderable — 54 precomputed frame strings (forward 8 → hold end 9 → backward 7 → hold start 30), 40 ms per frame, 6-step trail derived from the agent's base RGB via alpha falloff + 15% bloom, linear fade of inactive cells to `MIN_ALPHA=0.3`. Frames cross-checked byte-for-byte against OpenCode's `createFrames({ style: "blocks", direction: "bidirectional", width: 8, holdStart: 30, holdEnd: 9 })` output — all 54 match. `opentui-spinner` is not installed; Inkstone drives the tick loop itself (`onMount → setInterval → onCleanup`, same idiom as the existing braille spinner) and renders each cell as `<text fg={colorFn(frameIdx, i)}>{char}</text>` inside a `<box flexDirection="row">` (same pattern as OpenCode's `bg-pulse.tsx:115-125`). The older simple braille `Spinner` is preserved as a standalone importable component (`src/tui/components/spinner.tsx`) for future subagent-status reuse — not a speculative addition; the user explicitly requested it be kept. Trimmed from OpenCode's source: diamonds style, forward-only / backward-only modes, configurable color arrays, per-frame caching, and the `animations_enabled` KV fallback (Inkstone has no KV toggle).

## Completed

- [x] ChatGPT subscription (OpenAI Codex) as a second OAuth provider. pi-ai 0.69.0 already ships `loginOpenAICodex` / `refreshOpenAICodexToken` / `openaiCodexOAuthProvider` (`utils/oauth/openai-codex`), and `openai-codex-responses` stream + OAuth provider are auto-registered at pi-ai module load (`utils/oauth/index.js:30-36`), so this is a provider-shim + TUI-wiring task — no `registerApiProvider` or `registerOAuthProvider` calls needed. **Backend**: `src/backend/providers/openai-codex.ts` is a ~115-line `ProviderInfo` shim mirroring Kiro's posture — `inflight: Promise<OAuthCredentials | undefined> | null` memo for concurrent-refresh dedup, `refreshIfNeeded()` → `doRefresh()` delegates to pi-ai's `getOAuthApiKey("openai-codex", { "openai-codex": creds })`, which handles the expiry-check + refresh-token rotation in one call (`utils/oauth/index.js:110-130`). pi-ai's `getOAuthApiKey` **throws** on refresh failure (`:121-126`); the shim wraps the throw so Inkstone's `getApiKey()` no-throw contract (`pi-agent-core/dist/types.d.ts`) holds — catch branch clears creds, reports via `reportPersistenceError({ kind: "auth", action: "refresh", ... })`, returns `undefined`. On success `saveOpenAICodexCreds(result.newCredentials)` persists unconditionally (matches Kiro's `kiro.ts:66` — saving an unchanged blob through the atomic path is idempotent; the identity-skip optimization was removed after the post-implementation review flagged the inverted comment vs behavior). `listModels()` returns `getModels("openai-codex")` unfiltered (no on-demand analog to Bedrock's `anthropic.*` trap in pi-ai 0.69.0's Codex entries) and `[]` when signed out so `DialogModel` hides the rows. `defaultModelId: "gpt-5.4"` — flagship reasoning entry in pi-ai's generated registry. **Persistence**: `AuthFile.openaiCodex` added next to `kiro` with the same `z.custom<OAuthCredentials>(null-and-object-guard)` predicate; Zod's `strictObject` catches typos in the top-level keys, the predicate rejects `null` / primitives per the lessons from the H3/H4 audit that hardened Kiro. Three helpers (`loadOpenAICodexCreds` / `saveOpenAICodexCreds` / `clearOpenAICodexCreds`) route through the existing `ensureConfigDir` (0700) + `writeFileAtomic` (0600, `O_CREAT|O_EXCL|O_WRONLY` tmp + rename) + in-memory cache pattern. **TUI**: `login-openai-codex.tsx` drives pi-ai's callbacks — `onAuth({ url, instructions })` → `DialogAuthWait.show` (clickable URL + muted instructions + live `progress` signal); `onPrompt` → `DialogPrompt.show` as a post-failure fallback when `127.0.0.1:1455` couldn't bind (port busy / firewall) or the browser callback never came through. Deliberately does NOT wire `onManualCodeInput` (pi-ai's concurrent paste-lane racer): trying to show a paste prompt alongside the auth-wait on a single-dialog stack would hide the primary URL, and `onPrompt`'s post-failure fallback already covers the only scenario where paste is necessary. Cancellation propagates by throwing `"Login cancelled"` from `onPrompt` when `DialogPrompt.show` resolves `null` — pi-ai's outer `try/finally` closes the HTTP server cleanly. Subscription-gating catch branch detects the account-extract error via case-insensitive substring match on `"accountid"` / `"account_id"` (hardened from case-sensitive after the post-implementation review) and surfaces a targeted "ChatGPT Plus or Pro subscription required" toast — pi-ai throws `"Failed to extract accountId from token"` when the JWT lacks the `chatgpt_account_id` claim. `disconnect-openai-codex.ts` mirrors `disconnect-kiro.ts` — confirm → `clearOpenAICodexCreds` → active-provider rehome onto `DEFAULT_PROVIDER` when that fallback is itself connected (otherwise warning toast nudging `/models`). `manage-menu.tsx` dispatches on `provider.id` for both Kiro and Codex reconnect/disconnect branches; `provider/index.tsx` grows an `OWNED_CREDS_PROVIDERS = new Set(["kiro", "openai-codex"])` gate for the connected-row → manage-menu route (Bedrock still short-circuits since we can't honestly manage ~/.aws/ creds), and the disconnected-row dispatch routes `"openai-codex"` to `startOpenAICodexLogin`. Per-provider `if`/`else` branches deliberately NOT generalized into a `provider.login?()` / `provider.clearCreds?()` hook — the third owned-creds provider is the right trigger for that refactor (behavioral-guidelines third-trigger rule). **OSC 52 clipboard primitive**: ported after a follow-up UI-reference check against OpenCode's `component/dialog-provider.tsx:164-171` `AutoMethod` surfaced a `c`-to-copy binding on the OAuth wait screen — load-bearing for SSH / blocked-loopback / no-default-browser users who need to surface the URL to a different machine. `src/tui/util/clipboard.ts`'s `copyToClipboardOSC52` is a ~15-line OSC 52 writer (DCS passthrough for tmux/screen via `TMUX` / `STY` env probe) — OpenCode's full multi-platform clipboard (`clipboardy` + native subprocess) was deliberately NOT ported because Inkstone only needs short-URL copy today. `DialogAuthWait` now binds `c` to copy + "URL copied to clipboard" info toast; both Kiro and Codex flows inherit the binding because it lives on the shared wait dialog, not on the per-provider login helpers. **Tests**: `test/openai-codex-refresh.test.ts` (3 cases) mirrors `test/kiro-refresh.test.ts` — concurrent-dedup via `mock.module("@mariozechner/pi-ai/oauth", ...)` overriding `getOAuthProvider("openai-codex")` to return a stubbed `refreshToken` with a call counter + delay knob (load-bearing; overriding `openaiCodexOAuthProvider` alone would no-op because pi-ai's registry Map was populated at module-eval before `mock.module` could replace it — review simplification); no-throw on refresh failure; `isConnected()` false after clear. `test/openai-codex-default.test.ts` (1 case) pins `gpt-5.4` against pi-ai's live `getModels("openai-codex")` registry, so a future pi-ai rename/removal surfaces here before the agent module's own boot-throw. 3× back-to-back runs clean. `bun run ci` passes (199 tests across 24 files, up from 199 pre-PR — existing tests unaffected). Docs updated: new `openai-codex` row in `docs/ARCHITECTURE.md`'s shipping-providers table, new "OpenAI Codex (ChatGPT) login flow" + "DialogAuthWait — shared OAuth wait screen" + "Disconnect / manage menu" subsections documenting the `OWNED_CREDS_PROVIDERS` gate, the OSC 52 trade-off, and the per-provider branch shape. Deferred: pi-ai version bump to 0.71.1+ (would unlock `websocket-cached` transport for cheaper session reuse — out of scope here), speculative `listModels()` filter (no known Codex on-demand trap in 0.69.0), full multi-platform clipboard port (wait for a second copy use case).

## In Progress

_None._


## Known Issues

- [ ] Dependency advisories (upstream-blocked, `bun audit` surfaces them). 5 high, 4 moderate, 1 low as of 2026-05-01. The high cluster is `solid-js@1.9.9 → seroval-plugins@~1.3.0 → seroval<=1.4.0` (prototype pollution, RCE via JSON deserialization, 2× DoS, RegExp DoS). Not actionable without forking `@opentui/solid@0.1.104`, which pins `solid-js: 1.9.11` as an **exact-version peer dependency** (see `node_modules/@opentui/solid/package.json`). solid-js 1.10 doesn't exist (registry has 1.9.x and 2.0.0-experimental.N); overriding seroval to 1.5.x would violate solid-js's `~1.3.0` range check. Moderates: `file-type` (ASF parser infinite loop, via `@opentui/core → jimp`), `uuid<14` (buffer-bounds check in v3/v5/v6, via `pi-coding-agent`), `esbuild<=0.24.2` (dev-server CORS, via `drizzle-kit` — dev-only), `@anthropic-ai/sdk` (insecure default file permissions, via `pi-ai`). Low: `diff 6-8` DoS in parse/apply patch. Re-evaluate when `@opentui/solid` ships a version that relaxes the peer or upgrades `solid-js` past the seroval-vulnerable range. `bun audit` is exposed as `bun run audit` but NOT wired into `bun run ci` — it would fail-shut on every run with no actionable fix.

- [ ] Secondary page `"text"` format ships (see Completed) but no caller currently uses it. The first real consumer (subagent work output, plain logs, or structured data) will validate the contract. If the shape needs to widen further — e.g. `format: "jsx"` or a render callback — revisit then; today's markdown-vs-text split covers the documented near-term use cases.

- [ ] `openVaultFilePart` in `src/tui/util/file-part-handler.ts` is vault-specific — it hardcodes `VAULT_DIR` path resolution, `isInsideDir` sandbox check, and `readFileSync`. Extracted out of `UserPart` so the rendering layer is decoupled, but the handler itself still assumes every `file` part points at a vault-relative filesystem path. Won't generalize when other agents emit `file` parts pointing at non-vault paths or non-filesystem content. Full fix: widen `DisplayPart.file` with a `source` discriminator (e.g. `"vault" | "web" | ...`) and convert `file-part-handler.ts` into a dispatch table keyed on that field; touches the bridge type, the `parts` schema (`source` column), both current producers (reader's `/article` and `@`-mentions), and serialize/deserialize. Revisit when a second producer emits file parts from a non-vault source — premature today.

- [ ] Reader permission shift — with the `activeArticle`-less refactor, reader's permission rules are now static on the whole Articles zone (any article edit → frontmatter-only, any article write → blocked). Previously rules applied only to the *currently-active* article; other articles were effectively edit-free. Re-evaluate if reader ever needs bulk-editing historical articles — today's shape is stricter by default, which fits the primary reading workflow.
- [ ] Streaming text may still flash at top on first response (needs live testing)
- [ ] Click-to-refocus may not work in all terminal emulators
- [ ] pi-ai Usage type doesn't separate thinking tokens from output tokens
- [ ] Assistant messages persisted before the per-message footer change will render without a footer (no backfill)
- [ ] Slash-command parser is naive — see "Robust slash-command parsing" in Future Work. Messages that happen to start with `/article ` will still be consumed as commands under the reader agent, even when that wasn't the user's intent.
- [ ] `DialogSelect`'s `rows()` memo has a dormant off-by-one: it keys the "is this the first header?" check on the raw `grouped()` index, so a dialog that mixes an uncategorized bucket (empty-string key, index 0) with a categorized group (index ≥1) gets one extra spacer row in the scrollbox height calculation. No caller mixes today (DialogModel always-categorized; DialogProvider / DialogCommand / DialogTheme / DialogAgent / DialogVariant / reader's `pickFromList` never-categorized). Fix alongside the first caller that needs to mix — filtering empty buckets out of the accumulator is a 2-line change.
- [ ] EC2 IMDS-only Bedrock auth isn't detected by `isConnected()`; the call itself will still succeed but the Connect dialog shows "Not configured" and Models hides Bedrock. Workaround: set any AWS_* env var (e.g. `AWS_PROFILE=default`) to hint, or create a `~/.aws/config` file.
- [ ] DB migrations folder ships with the source tree but isn't yet bundled for a packaged binary — `migrate()` reads files off disk at `import.meta.dir/migrations`. Fine for `bun run dev`; needs a bundler trick (opencode inlines via `OPENCODE_MIGRATIONS` global) when Inkstone ships as a single executable.

## Future Work (Post-MVP)

- [ ] Move tool-arg summary format from the UI to the tool definition. **Partly done** — the per-tool renderers moved from `src/tui/util/tool-summary.ts` to `src/bridge/tool-renderers.ts` as `TOOL_ARG_RENDERERS: Record<string, (args) => string>`, so the UI no longer owns the catalog. The remaining step — attaching `renderArgs?: (args) => string` to each `AgentTool` object directly (so third-party agents supply their own format without editing the central map) — is still open. Complication: pi-coding-agent's `readTool`/`writeTool`/`editTool` are factory outputs Inkstone doesn't own; a cast-at-import intersection type (or upstream PR to pi-coding-agent) is needed to attach the field. Inkstone-owned tools (`updateSidebarTool`) would just grow a field. Revisit when (a) a second agent lands with its own tools, or (b) we need external agent authors to ship tool-specific renderers.

- [ ] Skills system — per-agent markdown bundles loaded from the vault. Layout: `$VAULT/InkStone/skills/<agent>/<skill>/SKILL.md`, YAML frontmatter `{ name, description }` + body. System prompt injects an `<available_skills>` block (name + description + location, composer pattern matches OpenCode / pi-coding-agent). Bodies read on demand via the existing `read` tool — no dedicated `skill` tool. `AgentInfo` grows `skills?: string[]`; composer emits block when non-empty. Deferred intentionally: no real skill content exists yet, and shaping the loader against zero candidates risks landing the wrong filtering model. Revisit when the first 2 real skill bundles exist (either a reader-side skill or the first KB skill).

- [ ] `AgentInfo.references?: string[]` — declarative doc-pointer list the composer turns into a "consult these files first" prompt fragment. Today agents hard-code doc paths in `buildInstructions()` strings (reader does this; KB would need `090 SYSTEM/099 LLM Wiki/{schema,policy,workflows/*}.md` when it lands). Revisit when KB arrives — three concrete use cases will validate the shape.

- [ ] `write: "deny"` zone policy — the matching rule kind (`blockInsideDirs`) shipped with the reader statelessness refactor and reader uses it today via `getPermissions`. A declarative `deny` zone would map to `blockInsideDirs` in `composeZonesOverlay`; would let agents express read-only-zones as data instead of writing a custom overlay. Small ergonomics win. Deferred per D8 until a second agent wants it.

- [ ] Move hard-coded vault paths to user configuration. Today `constants.ts` defines `ARTICLES_DIR`, `SCRAPS_DIR`, `NOTES_DIR`, `TEMPLATES_DIR` as `${VAULT_DIR}/<hard-coded path>`, and agent zone declarations hard-code the same vault-relative paths. Users with non-default vault layouts (different folder names, different numbering schemes) must edit code. Migration: make each agent's zones configurable via `$VAULT/InkStone/agents/<name>.json` (or similar), and derive `constants.ts` values from the same source. D7 (vault ≠ runtime state, amended) committed `$VAULT/InkStone/` as the convention root for this configuration.

- [x] Slash-command dropdown in the prompt textarea — shipped. See Completed section above and `docs/SLASH-COMMANDS.md` for design history.
- [ ] Memory files system — design exploration captured in `docs/MEMORY.md`. `user.md` + `memory.md` under `~/.config/inkstone/`, inlined by `composeSystemPrompt` after `BASE_PREAMBLE`. Read path is low-risk, ship when there's content that wants to live there; write path has real design questions (explicit vs auto, confirmation, overwrite semantics, review flow, per-agent gating) that want lived read-path experience to resolve.
- [ ] **Events table / timeline reader** — dropped the writers in the second review pass because they had no readers. Re-add when a history-dialog or timeline view actually needs model-change / thinking-level-change / summary events surfaced.
- [ ] **Tree / branching support** — `messages.parent_id` was dropped. Add back (or `turn_id`, depending on the requirement that lands first) when summarization needs turn grouping or when a branching UI arrives. Design it with real requirements, not speculatively.
- [ ] **Concurrency — double-launch protection.** Two Inkstone processes can attach to the same active session and interleave writes. Documented in `docs/SQL.md` § Known limitations. Fix options: advisory flock on `DB_FILE`, or `sessions.owner_pid` checked on attach. Not urgent — no fix until this bites someone.
- [ ] **Migration bundling for packaged builds.** `migrate()` reads from `import.meta.dir/migrations` at runtime; bundling to a single executable requires inlining (opencode's `OPENCODE_MIGRATIONS` global is the reference pattern). Not relevant until Inkstone ships outside `bun run dev`.
- [ ] Per-agent UI beyond prompt color (e.g., agent-specific sidebar info, icons)
- [ ] Mid-session agent switching (requires per-message agent stamping on user bubbles and tool-result routing rules — intentionally deferred)
- [ ] Effort-variant cycle keybind + slash command (OpenCode uses `ctrl+t` + `/variants`). Palette-only access ships; add when effort becomes a frequently-toggled setting.
- [ ] Extract a `MessageErrorPanel` component from `conversation.tsx:81-99` when the abort/error split above lands — at that point the inline block will need real branching (panel vs footer suffix), which justifies the component. Until then a single-variant panel has no polymorphism to pay for the extraction, so it stays inline (matches the "single-consumer, factor out on second consumer" convention called out in `docs/ARCHITECTURE.md:192`).
- [ ] Replace the local Bedrock on-demand filter with pi-ai's per-model on-demand flag when one is added upstream. Today `src/backend/providers/amazon-bedrock.ts` drops `anthropic.*`-prefixed IDs via `!startsWith("anthropic.")` because pi-ai doesn't expose an on-demand flag. Swap to an explicit positive check (e.g. `m.onDemand === true`, depending on the shape pi-ai ships) so `undefined` can't silently widen what `DialogModel` offers.
- [ ] More providers beyond Bedrock (Anthropic direct, OpenAI, etc.). When a provider needs user-supplied credentials (API key), add an `ApiMethod`-style input flow from OpenCode's `dialog-provider.tsx` that, on success, advances to `DialogModel` scoped to that provider (reintroduce the optional `providerId` prop that was removed in this PR).
- [ ] Custom providers that bring their own streaming transport (non-pi-ai). Extend `ProviderInfo` with an optional `streamFn` field wired into `Agent` construction. Not speculatively built.
- [ ] Multi-session support
- [ ] Session branching/forking
- [ ] Plugin system for custom agents
- [ ] Richer tool rendering (syntax-highlighted code blocks, expandable diffs)
- [ ] Reading progress indicator (stage display in header)
- [ ] Full theme system (33 themes, dark/light switching, custom themes)
- [ ] User-configurable keybinds + leader-chord support (extend `src/tui/util/keybind.ts` with a Zod override schema merged from `config.json`, and port OpenCode's `<leader>X` chord machinery in `tui/context/keybind.tsx`).
- [ ] KV persistence for settings (from OpenCode)
- [ ] Config file for debug/dev settings (e.g. `KIRO_LOG`, `KIRO_LOG_FILE`) — currently these must be passed as env vars on the command line; a dedicated config file (or a `[dev]` section in `config.json`) would let contributors opt in without editing `package.json`.
