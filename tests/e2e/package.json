{
	"name": "@inkstone/e2e",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "Full-system end-to-end Test Harness (ADR-0019): spawns a real Core serving the real Web Client in headless Chromium.",
	"scripts": {
		"check": "tsc --noEmit",
		"test:e2e": "playwright test",
		"test:e2e:install": "playwright install chromium"
	},
	"devDependencies": {
		"@inkstone/contract": "workspace:*",
		"@playwright/test": "^1.60.0",
		"@types/node": "^22.0.0",
		"effect": "^3.21.2",
		"typescript": "^5.0.0"
	}
}
